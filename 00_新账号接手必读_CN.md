# CD-C3DA 新账号接手必读

本文档是新 Codex 账号进入 `J:\nlp\CD-C3DA` 后的第一入口。不要根据聊天记忆猜测项目状态，必须以本目录中的文件、Git 提交、实验清单和实际运行产物为准。

## 1. 必读顺序

1. `00_新账号接手必读_CN.md`
2. `01_CD-C3DA项目完整介绍与迁移手册_CN.md`
3. `02_CD-C3DA实验工作流Skill_CN.md`
4. `实验记录与模型索引_CN.md`
5. `03_CD-C3DA下一阶段改进计划_CN.md`
6. `04_CD-C3DA双通道增强设计_CN.md`
7. `05_CD-C3DA双通道增强实施方案_CN.md`
8. `06_CD-C3DA最佳流程复现说明_CN.md`
9. `AGENTS.md`

根目录副本用于新账号快速阅读；项目 Skill 的正式文件仍是：

```text
docs\skills\c3da-experiment-workflow\SKILL.md
```

## 2. 当前不可争议的基线事实

- 正式主仓库：`J:\nlp\CD-C3DA`
- GitHub 远程：`https://github.com/chocolatelog-star/c3da.git`
- 当前最佳主分支：`master`
- 当前最佳主分支提交：`d2f2a35`
- 正式 GPU 验收代码提交：`558e4de`
- 主任务：跨域 ASTE，即从句子中抽取“方面词、观点词、情感极性”三元组。
- 当前重点方向：`rest16 -> laptop14`
- BGCA 论文基线 raw F1：`47.28`
- 当前完整从头可复现最佳 raw F1：`48.93`
- 当前 fixed F1：`50.21`
- 最新再次验收运行：`master-best-check-v1`
- 最新验收结果：raw P/R/F1=`58.31/42.14/48.93`，fixed F1=`50.21`
- 最新验收运行完成十个阶段，且没有读取历史运行产物。

## 3. 三个 C3DA 目录

### 3.1 `J:\nlp\C3DA-main`

这是最早下载和修改的论文原始代码副本，没有 `.git`，约 0.49 GB。它主要保存原始 C3DA 的方面级情感分类代码、早期 Windows 复现资料和原始数据。它不是当前跨域 ASTE 正式开发目录，不要在这里继续改主线代码或运行当前十阶段实验。

### 3.2 `J:\nlp\CD-C3DA`

这是唯一正式主仓库，连接 GitHub，保存当前代码、分支、工作树、配方、测试、实验记录和后续研究。所有新开发都必须从这里的 `master` 创建新分支，不得在另外两个目录继续演化代码。

### 3.3 `J:\nlp\CD-C3DA-native-best-rc-v1`

这是原生最佳流程的独立验收仓库，远程地址指向本地 `J:\nlp\CD-C3DA`。它固定在 `fix/native-best-training-semantic-v2` / `d2f2a35`，保存 `native-best-v2-training-semantic` 的十阶段验收现场。它只用于审计和证明当前代码能够从头复现 `48.93/50.21`，不作为后续开发目录，也不得作为新实验的上游输入。

## 4. 新账号开始工作前必须执行的只读检查

```cmd
cmd /c "J: && cd /d J:\nlp\CD-C3DA && git status --short --branch && git branch -avv && git worktree list"
```

检查目标：

- 明确当前分支，不把实验分支误认为 `master`。
- 确认工作区是否有用户未提交改动。
- 确认所需工作树和分支是否仍存在。
- 不得用 `git reset --hard`、`git checkout --` 或删除命令清理用户改动。

## 5. 新账号必须遵守的操作边界

- 修改前先完整阅读项目说明、实验索引和 Skill。
- 修改前说明目标、文件、参数变化和是否需要重跑，等待用户确认。
- 每次实验改动创建新分支；`master` 只保留完整 GPU 验证后的最佳版本。
- 正式实验只读取本次 `run_id` 内产物、声明模型和原始数据，禁止读取其他 `runs` 或历史工作树。
- 任何删除操作都必须再次获得用户明确许可。
- 正式训练默认由用户在 CMD 中运行；代码完成后提供一条完整单行命令。
- 所有训练、生成和过滤阶段必须显示进度并支持断点恢复。
- 用户显卡为 RTX 3070 8GB，默认训练批大小1、评估批大小2、梯度累积16、fp16 和梯度检查点。
- 下载模型只下载 PyTorch 版本，并放在 `J:\nlp\models`。
- 所有新增项目 Markdown 文档使用中文。
- 不启动不必要的多个智能体；普通修改由当前智能体直接完成。

## 6. 当前工作方向

当前最佳流程已经稳定复现。下一阶段不是继续修改生成器轮次，而是修复双通道增强的编辑契约和过滤闭环：

1. 清除 `[ASP]`、`[OPI]` 等控制占位符泄漏。
2. 保证计划的新方面、新观点和目标情感真实出现在生成文本和最终标签中。
3. 降低源域提取器对观点/中性增强的一票否决和错误改标签作用。
4. 先用150条增强验证正确性，再测试250条分层配额。
5. 候选版本只有超过对应基线并通过完整实验，才能请求合并到 `master`。

## 7. 禁止误解

- 421条基础高精度伪标签、494条完整伪标签、150条增强和1499条最终训练行是当前最佳运行的黄金观察值，不是为了匹配历史而强行截断所有未来实验的固定数量。
- `49.01/51.83` 是复用历史增强的诊断最高值，不是正式可复现主线。
- `48.93/50.21` 才是当前正式从头可复现最佳。
- 历史工作树和历史运行目录可用于审计，不能作为正式新实验输入。
- fixed F1 是辅助分析指标，主要比较指标是 raw F1。
