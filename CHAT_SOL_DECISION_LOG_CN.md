# CD-C3DA 研究决策日志

> 更新时间：2026-08-30 23:10（北京时间）
>
> 只记录改变研究路线、基线、门控或禁止范围的决定。具体小修复写入Git（版本管理）提交，不在此累积聊天历史。

## D01：正式最佳与研究方向

- `rest16 -> laptop14` 48.93为保护基线，已超过BGCA（双向生成跨域方法），不得被后续速度配置或迁移失败覆盖。
- `laptop14 -> rest15` 54.01为当前研究基线。
- 六方向共同目标仍是超过BGCA；当前先在`laptop14 -> rest15`筛选机制。

## D02：主瓶颈

单三元组能力不是首要问题。主瓶颈按优先级为：目标域方面/观点元素供给、多三元组完整结构形成、复杂结构生成召回、负面类别；中性仅作辅助。

## D03：固定流程边界

保留单生成器、`k=1`、目标真实句锚定、完整标签、NLI（自然语言推断）/exact（精确回抽）硬验证、联合配额、source-dev（源域开发集）选模和无金标结构门控。目标测试只用于正式最终报告。

## D04：关闭的训练路线

关闭双生成器、教师—学生、`k=2`、中性强加权、普通质量/结构调权、原锚回放、复杂样本重复呈现、NLL（负对数似然）排序/加权、EOS（终止符）系列、ECAL（元素覆盖辅助损失）系列、FGSM（快速梯度符号法）参数搜索、DANN（领域对抗网络）开关/调权和无供给证据的配对/计数损失。不得以新的参数网格重新开启。

## D05：句法图研究边界

依存/POS（词性）图和RGAT（关系图注意力网络）当前只用于目标伪标签形成；最终ASTE（方面级情感三元组抽取）暂时无图。任何图组件必须先通过入口审计、source-dev指标和目标无金标伪标签分析。

## D06：元素感知完整Treatment判定

2026-08-30服务器batch=1/4/8/16诊断完成。batch16相对普通RGAT使multi recall提高2.36个百分点、overall absence下降3.49个百分点，但没有同时超过无图T5。判定为`QUICK_DIAGNOSTIC`（快速诊断），不进入Graph Freeze（图模块冻结）或Phase B（阶段B）。

## D07：唯一下一变量

只批准Focus-only（仅聚焦）与Coverage-only（仅覆盖）组件归因，固定DANN=0、图结构、训练参数、数据和伪标签规则。不批准损失系数、batch、图层或注意力头网格。

## D08：服务器与执行分工

本地实现、CPU（中央处理器）测试、Codex Sol（高级工程模型）复审、提交和推送后，服务器按明确提交运行。除非用户主动要求代跑，Codex只给服务器单行命令，由用户启动。

## D09：2026-08-30项目清理

用户完成逐项精确授权后，重复文档、重复历史最佳、M1 v6、M1 v8/v9e重型失败模型、M1 v9空壳、旧RoBERTa调试权重、部分缓存和旧工作树已删除，合计释放约36.05 GB。两条正式最佳、当前元素感知工作树、组件测试、完整图缓存和M1 v8/v9e小型实验报告已验证保留。
