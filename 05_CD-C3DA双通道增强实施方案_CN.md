# CD-C3DA 目标锚定增强实施方案

> 更新时间：2026-08-13 15:30（北京时间）
> 正式实验在用户明确要求前不启动。

## 1. 已完成基础

目标锚定三步代码已经实现真实句局部编辑、硬契约、NLI（自然语言推断）与抽取器过滤、联合配额选择、质量分层、训练权重上限、阶段复用门禁和空候选失败保护。步骤3取得 raw/fixed F1 = 53.94/54.61，仍低于54.01/55.53正式基线，但证明部分机制有潜力。

## 2. 下一轮代码改动

1. 将 `max_per_base` 明确定义为每个锚句最多 `k` 条增强，并在配方、清单、分析和产物标签中统一使用 `anchor_max_augmentations=k`。
2. 增加 `k=1/2` 两份隔离配方，其他候选、过滤、训练参数保持一致。
3. `k=2` 时要求第二条来自不同编辑类型或不同结构缺口，并允许单独降权。
4. 把选择目标改为多三元组召回优先：多三元组、长尾方面、缺失观点、负面、中性依次进入软优先级。
5. 输出逐锚句候选数、选中数、编辑类型、三元组数、质量层、权重和拒绝原因。
6. 增加 FN/FP（假阴性/假阳性）离线分类，追踪方面、观点、配对、情感、边界和多三元组错误。

## 3. 测试要求

- `k=1/2` 上限测试。
- 同锚句第二条不同编辑类型/结构缺口测试。
- 有可行联合组合时选择器不得因贪心顺序少选。
- 选择后比例与每锚句上限再次验证。
- 空候选、过滤后为空、最终选中为空立即失败。
- 中性反向 NLI 为 entailment（蕴含）才可进入高层，neutral（中立）进入中层，contradiction（矛盾）拒绝。
- 目标测试金标不得出现在选择执行路径。
- 阶段复用父运行身份、语义哈希和外部输入哈希必须完整校验。

## 4. 实验顺序

第一轮只运行 `laptop14 -> rest15`、seed（随机种子）1000：

- E0：54.01正式基线。
- E1：目标锚定成功机制最小版，`k=1`。
- E2：与E1相同，`k=2`。

E1/E2均从一个完整父运行受控复用未改变阶段，只重跑增强及真实下游。优胜候选再完整从原始数据验收。

## 5. 晋级和停止条件

晋级要求：raw F1 >54.01，recall >51.34%，多三元组 recall >43.27%、F1 >50.21，precision >=56.5%，负面F1 >=49.0。中性不是硬门槛。

出现任一情况停止：编辑契约不满100%、目标金标参与选择、比例或 `k` 上限不满足、增强总权重超限、raw F1不升且精确率明显下降、复用来源无法单一解释。

## 6. 推广顺序

单方向通过后依次验证 `rest15 -> laptop14`、`laptop14 -> rest14`、`laptop14 -> rest16`。至少两个提升且第三个无不可接受回退，再运行六方向至少3个随机种子。不同方向允许独立 `k` 和数值参数，但共享相同方法结构。
