# CD-C3DA 六组跨域实验分析与 GPT 交接

> 更新时间：2026-08-30 23:10（北京时间）
>
> 用途：让Chat Sol（研究负责人）快速掌握项目目标、正式结果、已确认机制、关闭路线和当前唯一研究问题。本文不代替运行索引和具体任务单。

## 1. 项目简介

CD-C3DA研究跨域ASTE（方面级情感三元组抽取）：从源域标注与目标域无标签文本中学习，并在目标域抽取`(方面, 观点, 情感)`三元组。系统以T5-base（T5基础模型）为主干，当前为单生成器流程，不采用双生成器或教师—学生网络。

正式流程：源域抽取器 → 目标伪标签 → 标签到文本生成器 → NLI（自然语言推断）与exact（精确回抽）过滤 → 联合配额选择 → 源域/伪标签/增强加权训练 → 最终评估。

## 2. 六方向正式结果

| 方向 | CD-C3DA raw F1 | BGCA F1 | 差距 |
|---|---:|---:|---:|
| `rest14 -> laptop14` | 52.54 | 53.64 | -1.10 |
| `rest15 -> laptop14` | 45.27 | 45.69 | -0.42 |
| `rest16 -> laptop14` | **48.93** | 47.28 | **+1.65** |
| `laptop14 -> rest14` | 56.94 | 65.27 | **-8.33** |
| `laptop14 -> rest15` | **54.01** | 58.95 | -4.94 |
| `laptop14 -> rest16` | 61.55 | 64.00 | -2.45 |

唯一超过BGCA的是`rest16 -> laptop14`，该运行是保护基线。当前研究基线为`laptop14 -> rest15` 54.01。

## 3. 为什么与 BGCA 仍有差距

同方向`rest14 -> laptop14`的可比证据最清楚：C3DA P/R/F1=59.95/46.77/52.54，BGCA复现为56.14/51.38/53.65。C3DA精确率高3.81个百分点，但召回低4.61个百分点；C3DA单三元组F1为55.73，高于BGCA的54.82，而multi F1/召回只有50.43/40.90，低于BGCA的52.94/47.63。

因此主要差距不是单三元组分类，而是目标方面/观点供给和多三元组完整召回。历史覆盖审计也显示源域方面对目标测试的直接覆盖只有2.0%–11.1%，明显低于观点覆盖41.0%–59.5%。

`laptop14 -> rest14`落后8.33点，是高精确率、严重漏检最典型方向：历史最佳P/R约69.36/48.29，FN（假阴性）514。餐厅目标域包含大量源Laptop（笔记本）领域没有的方面节点，并且多方面句比例高；当前过滤和增强更擅长保守保真，不能补齐这些新方面和第二/第三三元组。BGCA的完整标签条件生成与等权增强在该方向提供了更强的完整结构覆盖，但其多个机制绑定，不能据单次复现断言双生成器本身是原因。

## 4. 已确认机制事实

1. 多三元组问题首先是元素/结构缺失，不是单纯配对错误。隐藏三元组审计中`opinion_missing`和完整遗漏明显多于配对缺失。
2. 复杂度是稳定风险轴。二三元组样本的完整吸收明显高于3+样本；重复呈现、回放和普通增权均不能修复。
3. 下游损失只能重新分配错误。NLL（负对数似然）、EOS（终止符）、ECAL（元素覆盖辅助损失）、FGSM（快速梯度符号法）等路线常减少FP但增加FN。
4. 强验证仍然必要。简单放宽伪标签会显著降低F1；应扩大候选来源而不是降低最终质量标准。
5. 中性类别困难但不是总体主差距。中性保持辅助，不采用强加权。

## 5. 当前图结构研究

M1在目标伪标签形成阶段加入Dependency/POS Graph（依存/词性图）、四头RGAT（关系图注意力网络）、Element Salience（元素显著性）和Multi-Element Coverage（多元素覆盖）。最终ASTE模型暂时完全不带图。

服务器快速诊断：

| 配置 | strict F1 | multi recall | absence | 合格伪标签/multi |
|---|---:|---:|---:|---:|
| 无图T5 | 57.84 | 47.64 | 69.77 | 508/185 |
| 普通RGAT | 56.57 | 45.75 | 73.26 | 557/209 |
| 完整元素感知batch16 | 56.58 | 48.11 | 69.77 | 552/209 |

完整元素感知相对普通RGAT有正向信号，但没有同时超过无图T5，因此当前不是Graph Freeze（图模块冻结）候选。下一步只做Focus-only（仅聚焦）和Coverage-only（仅覆盖）组件归因；禁止直接进入Phase B（阶段B）或目标测试。

## 6. 论文方法的借鉴边界

- BGCA：借鉴完整标签条件生成与回抽一致性，不恢复双生成器。
- RSDA（可靠自训练域适配）：借鉴结构组合、NLI验证和完整多方面监督。
- DAEGCN/DASA（图卷积/依存感知方法）：借鉴依存/POS拓扑和关系注意力，仅用于目标伪标签形成。
- C3DA/AG-CDSA（跨通道增强/对抗图式方法）：借鉴方面与观点通道分离、生成后过滤；不能直接迁移其分类任务结论。

任何论文启发都必须先经过本地无金标诊断，不能用论文结果替代CD-C3DA的因果消融。

## 7. 当前关闭和保留边界

关闭：双生成器、教师—学生、`k=2`、中性强加权、普通结构/质量调权、原锚回放、复杂样本重复呈现、NLL/EOS/ECAL系列、FGSM参数搜索、DANN调权、无供给证据的配对/计数损失。

保留：单生成器、`k=1`、目标真实句锚定、完整标签计划、NLI/exact硬验证、联合配额选择、source-dev（源域开发集）选模、无金标多结构门控、元素感知组件归因。

## 8. 当前唯一研究问题

在固定普通RGAT、数据、DANN=0、训练参数和伪标签规则下：

> 多三元组召回的改善主要来自元素显著性门控，还是多元素覆盖损失？

只批准两组新诊断：Focus-only与Coverage-only。Chat Sol需要在两组结果齐全后给出`KEEP_FOCUS`（保留聚焦）、`KEEP_COVERAGE`（保留覆盖）、`INTERACTION_ONLY`（仅组合交互）或`STOP_GRAPH_TUNING`（停止图调优）之一。不得在结果出现后临时降低门槛或追加系数网格。

## 9. 协作与数据边界

Codex负责代码、测试、运行命令和机器可读结果；Chat Sol负责研究判定。大量逐行结果可交Work Luna（重文件分析器）压缩为结果卡。目标测试金标只允许用于正式最终评估，不得用于组件、阈值、损失、配额或检查点选择。
