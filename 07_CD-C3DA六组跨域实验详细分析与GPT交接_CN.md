# CD-C3DA 六组跨域实验分析与 GPT 交接

> 更新时间：2026-08-13 17:04（北京时间）
> 用途：让外部 GPT（生成式预训练模型）快速掌握项目、证据、失败路线和当前研究问题，并提出可验证的改进方案。

## 0. ChatGPT—Codex 协作协议

- ChatGPT（聊天模型）只读分析实验、识别瓶颈并提出方案；Codex（代码智能体）核验本地代码和运行证据、向用户请示、实施、测试和回填；用户是唯一审批者。
- 每次分析先把结果标为 `DIAGNOSTIC`（诊断）、`QUICK_ABLATION`（快速消融）或 `FULL_RUN`（完整运行），再说明能支持和不能支持的结论。
- ChatGPT 固定返回：有效性、泄漏/复用风险、瓶颈证据、2–3条路线、推荐方案、唯一变量、复用/重跑阶段、停止条件、快速消融通过条件和完整运行晋级条件。
- Codex 同步字段包括：Git 提交/分支/差异；运行身份、方向、随机种子和研究问题；父运行、`reuse_depth` 和阶段边界；目标测试访问与参数选择依据；完整指标；候选—过滤—选中—训练漏斗；命令、配置、指标和日志路径及首次错误。没有的指标写“当前不可观测”。
- GitHub（远程仓库）是首选代码审计渠道；推送失败时，Codex 用本地 Git（版本管理）、文档和实验产物直接桥接必要信息。ChatGPT 不能把未推送代码视为已自行审计。
- ChatGPT 的建议不是批准任务；Codex 核验后必须先交用户确认，未批准不得改代码或启动实验。

## 1. 项目要解决什么

任务是跨域 ASTE（方面级情感三元组抽取）：源域有标注数据，目标域训练数据无标签，模型需要从目标测试句中抽取 `(方面, 观点, 情感)`。目标是六个跨域方向分别超过 BGCA（双向生成跨域方法）论文结果。

当前 CD-C3DA 使用 T5-base（T5 基础模型）的单生成器主线：源域抽取器生成目标伪标签；标签到文本生成器构造增强；过滤与完整双三元组补全后，将源域、目标伪标签和增强数据加权用于最终训练。最终模型还包含 DANN（领域对抗网络）和情感对比学习。项目不采用双生成器或教师—学生网络。

## 2. 六方向结果

BGCA 是论文五次运行均值，CD-C3DA 是 seed（随机种子）1000 的正式可复现最佳。

| 方向 | CD-C3DA | BGCA | 差距 | 主要问题 |
|---|---:|---:|---:|---|
| `rest14 -> laptop14` | 52.54 | 53.64 | -1.10 | 中性弱、扩大观点增强会挤压方面并增加误检 |
| `rest15 -> laptop14` | 45.27 | 45.69 | -0.42 | 总召回和伪标签质量偏低，多三元组 F1 42.76 |
| `rest16 -> laptop14` | **48.93** | 47.28 | **+1.65** | 唯一超过；仍依赖精确率，召回只有42.14 |
| `laptop14 -> rest14` | 56.94 | 65.27 | **-8.33** | P/R=69.36/48.29，最大问题是漏检和多三元组 |
| `laptop14 -> rest15` | **54.01** | 58.95 | -4.94 | 权重0.75改善转化，剩余 FN 主要来自多三元组 |
| `laptop14 -> rest16` | 61.55 | 64.00 | -2.45 | 整体较强，负面与中性仍弱 |

跨方向共同证据：源域方面词对目标测试三元组的直接覆盖只有2.0%–11.1%，观点词覆盖为41.0%–59.5%；高精度目标伪标签中的中性几乎为0；多三元组句贡献大量 FN（假阴性）。因此核心问题是目标方面发现、完整三元组召回和可靠数据增强，而不是单纯情感分类。

## 3. 当前最重要的两条基线

### 3.1 保护基线

`rest16 -> laptop14` raw P/R/F1 = 58.31/42.14/48.93，fixed F1 = 50.21，超过 BGCA 1.65。当前主分支 `master=d2f2a35`，正式 GPU（图形处理器）验收代码 `558e4de`。独立验收现场只用于审计，不作为新实验输入。

### 3.2 研究基线

`laptop14 -> rest15` 伪标签权重0.75完整从头运行：raw P/R/F1 = 56.98/51.34/54.01，fixed F1 = 55.53；正面/负面/中性 F1 = 58.20/49.46/0；单/多三元组 F1 = 58.04/50.21，多三元组召回43.27%。

它相对0.65锚点提高2.81，说明目标伪标签信号原来偏弱；0.80只得到53.62，说明权重存在平衡点，不能继续靠增权解决剩余4.94差距。

## 4. 最近四次增强实验说明了什么

| 实验 | raw/fixed F1 | 结论 |
|---|---:|---|
| 54.01权重0.75完整基线 | **54.01/55.53** | 当前直接对照 |
| 目标锚定步骤1 | 52.38/53.29 | 真实句锚定与强过滤可运行，但信号覆盖不足 |
| 目标锚定步骤2 | 52.29/53.38 | 缺口感知提高召回，但精确率下降抵消收益 |
| 目标锚定步骤3 | 53.94/54.61 | 质量分层恢复精确率，多三元组改善；距离raw基线仅0.07，但未超过 |

应保留的机制：

- 从目标域真实句出发进行局部编辑。
- 强编辑契约，未完成编辑不能被抽取器重新贴标保留。
- 联合考虑编辑类型、多三元组、质量和锚句上限的选择求解。
- 高/中质量分层权重及增强总权重上限。
- 每个锚句最多选择 `k` 条增强，首轮比较 `k=1/2`，控制高度相关样本重复。

不应保留的做法：

- 固定扩大增强总数并强制填满。
- 把中性作为主要配额或靠损失强增权。
- 仅依赖源域精度或生成器分数判断目标域质量。
- 在多个方向直接复制同一伪标签权重。

## 5. 已经失败或暂停的路线

- 简单放宽伪标签：raw F1 44.44。
- 中性损失强增权：raw F1 43.18。
- pairing loss（配对损失）：raw F1 46.49，精确率升但召回降。
- coverage loss（覆盖损失）：raw F1 44.37。
- 三任务 mixed generator（混合生成器）：raw F1 44.07。
- 动态比例扩大到336条：raw F1 50.25，误检和有效总权重膨胀。
- 目标方面发现 v2：候选召回23.21%，但精度只有48.15%，没有注入训练。
- 多候选最终解码：可补4个多三元组 TP（真阳性），但总体45.59仍低于 BGCA 45.69。
- 生成器25轮最后检查点：47.30，不优于8轮最佳检查点主线。
- 将伪标签权重0.75直接迁移到 `rest16 -> laptop14`：45.90，低于48.93。

## 6. 当前五模块主线

1. 目标域真实句锚定：局部方面/观点编辑；每锚句最多 `k` 条，`k` 按方向实验决定。
2. 多三元组与召回导向候选：优先缺失三元组、长尾方面、缺失观点和负面。
3. 局部编辑契约与分层验证：跨度、情感、未编辑三元组和控制符硬检查；抽取器只作支持。
4. 缺口感知软配额与分层权重：合格多少用多少，不低质补齐；限制同锚句相关样本和增强总权重。
5. 方向自适应训练：方法结构一致，但 `k`、配额和伪标签权重可按方向校准。

详细计划见 `03_CD-C3DA下一阶段改进计划_CN.md`。

## 7. 希望 GPT 重点回答的问题

请基于以上证据给出可做单变量消融的方案，重点回答：

1. 如何构造能提高多三元组召回、同时限制 FP 的目标域局部增强？
2. 每锚句 `k=1/2` 如何与编辑类型、多三元组缺口和样本权重联合设计？
3. 如何在不读取目标测试金标的前提下估计“新增三元组是真补全还是误检”？
4. 对 `laptop14 -> rest14` 这种高精确低召回方向，优先补方面、观点还是配对，如何设计判别实验？
5. 有哪些轻量方法能提升目标域长尾方面覆盖，并适配 RTX 3070 8GB？
6. 如何使同一增强方法适配六方向，同时避免共享一组数值参数？

期望每个建议包含：作用假设、最小代码改动、最早受影响阶段、可复用阶段、单方向验证指标、失败条件和推广条件。

## 8. 不可违反的实验约束

- 目标测试金标只能用于最终离线评估，不得用于候选、阈值、配额、`k` 或检查点选择。
- 快速消融只能从一个完整从头父运行受控复用，禁止复用链和多个父运行混合。
- 新最佳、BGCA正式比较、六方向扩展和合并主分支前必须完整从原始数据验收。
- raw F1 是主指标；必须同时报告 P/R、正负中性、单/多三元组和 FN/FP。
- 不采用教师—学生网络或双生成器；中性只作为辅助改善目标。
- 单卡 RTX 3070 8GB，训练批次1、评估批次2、梯度累积和半精度按当前配方控制。
- 未经许可不删除任何模型、检查点或实验目录。
