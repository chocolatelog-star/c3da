# CD-C3DA 六组跨域实验详细分析与 GPT 交接

本文用于把 CD-C3DA 项目、当前流程、六组跨域实验结果、失败经验和待解决问题一次性交给外部 GPT（生成式预训练模型）分析。目标不是复现已有结论，而是请外部模型基于完整证据提出能够让六个跨域方向分别超过 BGCA 的改进方案。

## 1. 项目简介

本项目研究跨领域 ASTE（方面—观点—情感三元组抽取）。每个三元组包含：

```text
(aspect, opinion, sentiment)
(方面词, 观点词, 情感极性)
```

例如：

```text
The battery life is very good.
<pos> battery life <opinion> good
```

跨领域设置中，模型只能使用源领域有标签数据和目标领域无标签训练数据，最终在目标领域测试集上评估。四个数据集为：

- `laptop14`：笔记本电脑评论。
- `rest14`：2014年餐馆评论。
- `rest15`：2015年餐馆评论。
- `rest16`：2016年餐馆评论。

六个跨域方向为三个餐馆数据集分别迁移到 `laptop14`，以及 `laptop14` 分别迁移到三个餐馆数据集。

项目正式仓库为 `J:\nlp\CD-C3DA`。当前可复现最好方向是 `rest16 -> laptop14`，raw F1（原始 F1）为48.93，超过 BGCA 同方向47.28，是六个方向中唯一超过 BGCA 的方向。终极目标是六个方向分别超过对应 BGCA，而不是只提高六组平均分。

## 2. 当前模型和十阶段流程

当前流程不是两个生成器，而是“一个抽取器 + 一个生成器 + 两个增强通道”。抽取器和生成器都以本地 T5-base（T5基础模型）为基础。

| 阶段 | 模块 | 主要作用和参数 |
|---|---|---|
| 1 | prepare（数据准备） | 准备源域标注数据、目标域无标签数据、目标测试数据和生成器训练数据 |
| 2 | extractor（抽取器） | T5-base；25轮；选择 last（最后检查点）；训练批次1、评估批次2、梯度累积16、fp16（半精度） |
| 3 | pseudo（伪标签） | 抽取目标域无标签数据；高精度层限制单三元组和方面—观点距离不超过5 |
| 4 | generator（生成器） | 单个 label-to-text（标签到文本）T5生成器；8轮；选择 best（最佳检查点） |
| 5 | augment（增强） | 同一个生成器服务方面掩码通道和观点情感掩码通道；NLI（自然语言推断）和抽取器回抽过滤 |
| 6 | prepare_final（最终数据准备） | 在同一运行编号内准备下游数据，禁止读取其他历史运行产物 |
| 7 | complete_multi2（完整双三元组） | 在高精度伪标签基础上补充完整双三元组，新增样本权重0.25 |
| 8 | build_final_train（组装最终训练集） | 源域标注 + 目标伪标签 + 增强数据 |
| 9 | final_train（最终训练） | 5轮；伪标签权重0.65；DANN（领域对抗网络）0.03；情感对比损失0.01 |
| 10 | evaluate（评估） | 4束搜索；最大96词元；同时报告 raw（原始）和 fixed（后处理修正）结果 |

当前最好流程使用 RTX 3070 8 GB：训练批次1、评估批次2、梯度累积16、fp16（半精度）、gradient checkpointing（梯度检查点）和 CUDA 0（英伟达计算平台0号卡）。所有新正式实验必须从原始数据重新训练，不能读取历史运行中的模型、伪标签或增强数据。

## 3. 比较口径

- 主指标是 raw F1（原始 F1）；fixed F1（后处理修正 F1）只作辅助诊断。
- BGCA 数值来自论文报告；项目六组当前结果主要是 `seed=1000（随机种子1000）` 单次运行。
- 因此下表适合判断差距和错误结构，不能视为与论文多次运行结果具有同等统计强度。
- 候选方案通过单个随机种子筛选后，最终必须运行至少3个随机种子并报告均值和标准差。

## 4. 六组最终指标总览

| 方向 | BGCA F1 | raw P（原始精确率） | raw R（原始召回率） | raw F1（原始 F1） | fixed F1（修正 F1） | 相对 BGCA | TP/FP/FN（正确/误检/漏检） |
|---|---:|---:|---:|---:|---:|---:|---:|
| `rest15 -> laptop14` | 45.69 | 53.25 | 39.37 | 45.27 | 48.09 | -0.42 | 213/187/328 |
| `rest14 -> laptop14` | 53.64 | 59.95 | 46.77 | 52.54 | 54.11 | -1.10 | 253/169/288 |
| `rest16 -> laptop14` | 47.28 | 58.31 | 42.14 | **48.93** | **50.21** | **+1.65** | 228/163/313 |
| `laptop14 -> rest15` | 58.95 | 55.01 | 48.66 | 51.64 | 53.17 | -7.31 | 236/193/249 |
| `laptop14 -> rest16` | 64.00 | 65.85 | 57.78 | 61.55 | 62.45 | -2.45 | 297/154/217 |
| `laptop14 -> rest14` | 65.27 | 69.36 | 48.29 | 56.94 | 58.08 | -8.33 | 480/212/514 |

六组 raw F1（原始 F1）平均为52.81，BGCA 平均为55.81，平均差距约-2.99。唯一超过 BGCA 的 `rest16 -> laptop14` 并不是绝对分数最高的方向；它能够超过主要因为该方向 BGCA 门槛只有47.28。绝对能力最强的是 `laptop14 -> rest16` 的61.55，但其 BGCA 门槛为64.00。

## 5. 目标数据规模与标签结构

| 方向 | 源域训练行 | 目标无标签行 | 目标测试行/三元组 | 测试正面/负面/中性三元组 | 单/多三元组测试行 |
|---|---:|---:|---:|---:|---:|
| `rest15 -> laptop14` | 605 | 906 | 328/541 | 364/114/63 | 184/144 |
| `rest14 -> laptop14` | 1266 | 906 | 328/541 | 364/114/63 | 184/144 |
| `rest16 -> laptop14` | 857 | 906 | 328/541 | 364/114/63 | 184/144 |
| `laptop14 -> rest15` | 906 | 605 | 322/485 | 317/143/25 | 210/112 |
| `laptop14 -> rest16` | 906 | 857 | 326/514 | 407/78/29 | 192/134 |
| `laptop14 -> rest14` | 906 | 1266 | 492/994 | 773/155/66 | 206/286 |

`laptop14 -> rest14` 的目标测试集最大，而且多三元组行286条，超过单三元组行206条。这解释了为什么该方向虽然精确率达到69.36，仍因514个漏检和48.29的召回率落后 BGCA 8.33个百分点。

## 6. 情感和结构指标

| 方向 | 正面 F1 | 负面 F1 | 中性 F1 | 单三元组 F1 | 多三元组 F1 | 多三元组召回率 |
|---|---:|---:|---:|---:|---:|---:|
| `rest15 -> laptop14` | 47.22 | 52.58 | **0.00** | 49.07 | 42.76 | 33.89 |
| `rest14 -> laptop14` | 55.87 | 57.14 | 3.03 | 55.73 | 50.43 | 40.90 |
| `rest16 -> laptop14` | 51.54 | 55.05 | 3.03 | 53.05 | 46.13 | 35.85 |
| `laptop14 -> rest15` | 56.95 | **45.23** | **0.00** | 57.27 | **46.25** | 39.27 |
| `laptop14 -> rest16` | 65.13 | 55.13 | **0.00** | 61.76 | 61.40 | 53.11 |
| `laptop14 -> rest14` | 60.33 | 54.42 | **0.00** | 63.01 | 54.93 | 44.16 |

共同现象：中性类别几乎失效，多三元组通常弱于单三元组。`laptop14 -> rest16` 是结构上最均衡的例外，单/多三元组 F1 分别为61.76/61.40，因此它更适合通过负面和中性专项改进缩小最后2.45个百分点，而不应大改结构模块。

## 7. 伪标签质量与完整双三元组模块

| 方向 | 高精度伪标签条数/F1 | 补全后条数/F1 | 新增双三元组行 | 最终训练行 |
|---|---:|---:|---:|---:|
| `rest15 -> laptop14` | 389/48.94 | 475/50.16 | 86 | 1228 |
| `rest14 -> laptop14` | 421/54.38 | 536/53.62 | 115 | 1950 |
| `rest16 -> laptop14` | 421/50.35 | 494/51.08 | 73 | 1499 |
| `laptop14 -> rest15` | 315/56.77 | 451/61.17 | 136 | 1505 |
| `laptop14 -> rest16` | 451/58.99 | 630/60.66 | 179 | 1684 |
| `laptop14 -> rest14` | 625/54.89 | 940/57.97 | 315 | 1993 |

关键判断：

1. 完整双三元组模块通常提高伪标签 F1，尤其是 `laptop14 -> rest15` 的56.77到61.17，以及 `laptop14 -> rest14` 的54.89到57.97。
2. 伪标签提升不一定转化为最终模型提升。`laptop14 -> rest15` 的补全伪标签 F1 达61.17，但最终只有51.64，说明问题可能发生在伪标签与源域样本的联合训练、领域冲突、权重或最终生成解码阶段。
3. `rest14 -> laptop14` 补全后伪标签 F1 从54.38略降到53.62，但最终表现仍是三个餐馆到笔记本方向中最高，说明不能只用伪标签隐藏标注 F1 决定是否保留一个模块。
4. 六组基础高精度伪标签中的中性样本均为0；补全模块虽然能引入部分中性三元组，但没有让最终中性能力稳定出现。

## 8. 增强流程通过率和最终组成

| 方向 | 生成请求 | 一致性后 | NLI后 | 抽取器后候选 | 最终方面/观点行 | 增强正/负/中性三元组 |
|---|---:|---:|---:|---:|---:|---:|
| `rest15 -> laptop14` | 1269 | 786 | 699 | 214 | 111/39 | 126/63/0 |
| `rest14 -> laptop14` | 1397 | 927 | 845 | 286 | 94/56 | 79/67/47 |
| `rest16 -> laptop14` | 1267 | 880 | 774 | 273 | 116/34 | 149/66/14 |
| `laptop14 -> rest15` | 997 | 648 | 579 | 169 | 115/35 | 141/54/1 |
| `laptop14 -> rest16` | 1525 | 995 | 865 | 266 | 134/16 | 158/38/0 |
| `laptop14 -> rest14` | 2328 | 1522 | 1353 | 414 | 130/20 | 143/56/5 |

抽取器过滤后的保留率约为29.2%到35.3%。这说明增加候选生成次数有合理性，但旧流程把通过候选统一排序后固定取150条，使不同方向的通道和情感分布差异很大。例如 `laptop14 -> rest16` 最终只有16条观点增强，`rest14 -> laptop14` 有56条；旧流程并没有显式保证不同目标域所需的结构和情感覆盖。

## 9. 领域词汇迁移证据

下面的覆盖率按规范化后的完全字符串匹配计算：统计目标测试三元组的方面词或观点词是否在源域训练三元组中出现。它不是语义相似度，只用于衡量直接词汇迁移难度。

| 方向 | 源域方面词覆盖目标测试三元组 | 源域观点词覆盖目标测试三元组 |
|---|---:|---:|
| `rest15 -> laptop14` | 2.2% | 41.0% |
| `rest14 -> laptop14` | 5.7% | 59.5% |
| `rest16 -> laptop14` | 2.0% | 44.4% |
| `laptop14 -> rest15` | 8.5% | 49.5% |
| `laptop14 -> rest16` | 11.1% | 57.4% |
| `laptop14 -> rest14` | 11.0% | 50.9% |

方面词覆盖只有2.0%到11.1%，明显低于观点词的41.0%到59.5%。因此跨域瓶颈不能只解释为情感比例不合适；目标域方面发现和方面—观点配对很可能是更基础的问题。餐馆到笔记本方向的方面词差异尤其大。

## 10. 每个方向的主要问题归因

### 10.1 `rest15 -> laptop14`

- 只差 BGCA 0.42，但召回率只有39.37，是六组最低。
- 源域训练数据只有605行，明显少于 `rest14` 的1266行和 `rest16` 的857行。
- 高精度伪标签 F1 只有48.94；增强中性三元组为0。
- 方面词直接覆盖2.2%，多三元组召回33.89%。
- 优先方向：目标方面发现、多候选伪标签一致性、可靠中性最小对比样本、多三元组完整性。

### 10.2 `rest14 -> laptop14`

- 距离 BGCA 仅1.10，是当前最适合快速验证新方案的方向。
- 伪标签 F1 54.38、观点词覆盖59.5%，均优于另外两个餐馆到笔记本方向。
- 主要剩余问题是中性 F1 3.03和多三元组召回40.90。
- 最新观点契约供给150条实验把观点增强从33条提高到77条、方面增强从117条降到73条，raw F1 从52.60降到51.94；说明增加观点比例会挤占方面信号并伤害正负召回。
- 优先方向：动态通道比例、质量分层权重、方面候选保护、中性候选强过滤。

### 10.3 `rest16 -> laptop14`

- 当前 raw F1 48.93，超过 BGCA 1.65；这是唯一已超过的方向，必须保护。
- 它不是绝对能力最强：召回42.14、多三元组召回35.85、中性 F1 3.03。
- 方面词覆盖只有2.0%，伪标签 F1 50.35也低于 `rest14 -> laptop14`。
- 超过 BGCA 的重要原因是该方向论文门槛较低，而不是所有瓶颈已经解决。
- 该方向的独立验收现场不能作为新实验输入；候选方案最终只在隔离运行中验证是否保持领先。

### 10.4 `laptop14 -> rest15`

- 距离 BGCA 7.31，是困难方向之一。
- 完整伪标签 F1 61.17很高，但最终模型只有51.64，存在明显的“伪标签质量未转化”问题。
- 负面 F1 45.23、多三元组 F1 46.25，分别是情感和结构的主要弱点。
- 目标无标签数据只有605行，目标信号也相对有限。
- 优先方向：检查伪标签权重与源域梯度冲突、负面观点边界、多三元组联合配对、最终解码漏检。

### 10.5 `laptop14 -> rest16`

- raw F1 61.55，距离 BGCA 2.45，是绝对性能最强、最接近结构均衡的方向。
- 精确率65.85、召回57.78，单/多三元组 F1 61.76/61.40。
- 主要缺口集中在负面 F1 55.13和中性 F1 0，而不是多三元组结构。
- 最终增强只有16条观点、0个中性三元组，情感覆盖明显不足。
- 优先方向：高质量负面和中性观点增强；保持现有方面和多三元组结构，不做大范围架构改动。

### 10.6 `laptop14 -> rest14`

- 距离 BGCA 8.33，是差距最大的方向。
- 精确率69.36为六组最高，但召回仅48.29；514个 FN（漏检）远多于212个 FP（误检）。
- 目标测试集有994个三元组和286个多三元组行，长尾与结构复杂度最高。
- 完整伪标签增加315行并把隐藏标注 F1 提高到57.97，但最终仍只有56.94。
- 优先方向：目标长尾方面发现、召回导向的多候选伪标签、三元组完整解码、降低源域笔记本方面词对餐馆预测的约束。

## 11. 已经失败或不应直接重复的路线

1. 简单放宽伪标签：`hp2` 实验 raw F1 44.44，引入噪声。
2. 中性类别强增权：raw F1 43.18，没有解决中性召回并伤害正负类。
3. mixed generator（混合任务生成器）：raw F1 44.07，混合任务削弱主要生成目标。
4. coverage loss（覆盖损失）辅助头：raw F1 44.37，没有转化为生成收益。
5. pairing loss（配对损失）：raw F1 46.49，精确率提高但召回下降。
6. 双三元组权重从0.25提高到0.35：raw F1 45.74，权重过高。
7. 无条件加入三元组以上伪标签：raw F1 45.38，噪声和欠配对明显。
8. 生成器从8轮最佳检查点改成25轮最后检查点：raw F1 47.30，训练更久本身没有优势。
9. 观点软过滤250条：raw F1 51.04，编辑目标未保持且正面信号膨胀。
10. 观点契约供给150条：raw/fixed F1（原始/修正 F1）51.94/54.45；生成正确性已解决，但观点候选挤占方面候选，正负和多三元组下降。
11. 动态比例与质量分层：raw/fixed F1（原始/修正 F1）50.25/53.82；增强数量和有效权重翻倍，同源自洽过滤未识别不自然文本，FP（假阳性）与多三元组误检增加。

## 12. 动态比例与质量分层正式实验结果

代码提交 `e49bfb6` 实现的动态比例和质量分层已经完成 `rest14 -> laptop14`、seed（随机种子）1000正式 GPU（图形处理器）实验：

- 仍使用一个8轮最佳生成器，不改成双生成器。
- 每个基础样本产生3轮不同编辑请求。
- 强过滤要求编辑契约、标签词覆盖、NLI（自然语言推断）非矛盾、抽取器支持和观点支持。
- 不固定最终增强总数；每个基础样本最多保留1条。
- 比例范围：方面60%到75%，非中性观点15%到30%，中性观点8%到15%。
- 目标比例：67%/20%/13%。
- 高/中/探索质量层权重：0.25/0.18/0.08。
- 正式运行共生成3661个请求，NLI（自然语言推断）后2939条、抽取器过滤后1199条、强过滤合格673条，最终保留336条。
- 最终方面/非中性观点/中性观点为202/84/50，即60.1%/25.0%/14.9%；实际落在方面下限和中性上限附近，没有达到67%/20%/13%目标。
- 高/中/探索层为215/21/100；增强有效权重总和65.53，是旧150条统一0.20权重总和30.0的2.18倍。

最终 raw P/R/F1（原始精确率/召回率/F1）为55.88/45.66/50.25，fixed F1（修正F1）为53.82。相对52.60/55.09对照分别下降2.35/1.27，相对 BGCA 53.64低3.39，未通过第一门槛。高精度伪标签421条、F1 54.38与完整伪标签536条、F1 53.62和对照完全相同，因此首次偏差在增强阶段。

与可复现基线相比，TP（真阳性）从253降到247，FP（假阳性）从169增到195，FN（假阴性）从288增到294。正面/负面 F1 从55.87/57.14降到53.77/54.15；中性 F1 从3.03升到7.79，但只增加2个TP并增加9个FP；多三元组F1从50.43降到47.22，其中精确率从65.77%降到59.32%。主要失败不是“方面比例太低”本身，而是增强数量和有效贡献翻倍后放大了噪声。

质量层分数只保证编辑标签、NLI和同一抽取器之间自洽，不能保证句子自然。多条被判定为高质量、NLI蕴含、抽取器完全匹配的直接编辑存在明显语病，例如 `I am difficulty if it was the drive itself`、`Then it poor charging at all`。因此本路线不扩展到其他方向，也不能把当前 high（高质量）层直接作为真正高质量数据。下一轮应把“候选发现”和“独立质量验证”解耦，优先设计目标域方面候选发现，而不是继续扩大现有增强数量。

## 13. 希望外部 GPT 重点回答的问题

请基于以上证据提出方案时回答以下问题：

1. 六个方向的共同瓶颈和方向特有瓶颈分别是什么？哪些模块可以共用，哪些参数必须按目标域自适应？
2. 如何提高目标域方面词发现能力，同时避免简单放宽伪标签导致精确率崩溃？
3. 为什么 `laptop14 -> rest15/rest14` 的完整伪标签质量提升没有充分转化为最终模型提升？应如何诊断源域梯度、伪标签权重、领域对抗和最终解码之间的冲突？
4. 如何恢复中性类别，而不重复“中性强增权导致总体下降”的失败路线？
5. 多三元组召回应优先从数据构造、训练目标还是解码策略改进？怎样避免三元组以上噪声？
6. 当前“单生成器多请求 + 动态比例 + 强过滤 + 分层权重”方案是否合理？比例、阈值和权重应如何根据无标签目标域信号自适应，且不能使用目标测试标签调参？
7. 是否值得引入教师—学生一致性、模型集成、不确定性估计、检索增强、领域词汇图或更明确的方面发现模块？请按照预期收益、实现成本和3070 8 GB可行性排序。
8. 请给出最小消融矩阵：每次只改变一个核心变量，先用哪些三个代表方向筛选，再怎样扩展到六方向和三个随机种子？
9. 每个方向要超过 BGCA，建议设置哪些中间门槛和停止条件？不能只用六组平均提升掩盖单方向失败。
10. 请明确指出可能的数据泄漏风险，尤其禁止使用目标测试标签决定训练比例、候选或超参数。

## 14. 外部方案必须遵守的约束

- 不修改或污染当前48.93最佳复现现场。
- 不读取其他历史运行的模型、伪标签或增强数据作为新实验输入。
- 正式实验必须在同一运行编号内从原始数据重新训练并支持断点恢复。
- 只能使用本地 PyTorch（深度学习框架）模型；基础模型默认位于 `J:\nlp\models`。
- 单卡 RTX 3070 8 GB；训练批次1、评估批次2、梯度累积16、fp16（半精度）和梯度检查点。
- raw F1（原始 F1）是主指标；必须同时报告精确率、召回率、三种情感和单/多三元组指标。
- 候选方案先单个随机种子筛选，最终六方向至少3个随机种子。
- 最终成功标准是六个方向分别超过对应 BGCA。

## 15. 关键版本和历史文件

| 项目 | 位置或版本 |
|---|---|
| 正式仓库 | `J:\nlp\CD-C3DA` |
| 原始论文代码副本 | `J:\nlp\C3DA-main`，只读参考 |
| 48.93/50.21最佳验收现场 | `J:\nlp\CD-C3DA-native-best-rc-v1`，禁止作为新实验输入 |
| 当前最佳主分支 | `master = d2f2a35` |
| 正式 GPU 验收代码 | `558e4de` |
| 五组基线汇总 | `J:\nlp\CD-C3DA\runs\reproducible\_suites\cross_domain_baseline_five_v1\phase1-five-seed1000-v1\results_cross_domain_baseline_five_v1.json` |
| `rest16 -> laptop14` 最佳运行 | `J:\nlp\CD-C3DA\runs\reproducible\rest16_to_laptop14_best_v1\master-best-check-v1` |
| 动态比例分层实现 | `feature/opinion-constrained-edit-quota-v1` / `e49bfb6` |

外部 GPT（生成式预训练模型）提出方案时，应优先给出可证伪、可逐步消融、能在单卡8 GB显存运行的实验设计，而不是一次性更换整个架构。
