# CD-C3DA 六组跨域实验分析与 GPT 交接

> 更新时间：2026-08-24 17:10（北京时间）
> 用途：让外部GPT（生成式预训练模型）快速掌握项目事实、核心证据、关闭路线和唯一下一步。详细门槛以`03_CD-C3DA下一阶段改进计划_CN.md`为准，完整运行历史以`实验记录与模型索引_CN.md`为准。

## 0. ChatGPT—Codex协作边界

- ChatGPT负责只读研究分析：判断实验身份、证据强度、泄漏与复用风险，给出最多3条路线和唯一推荐。
- Codex负责核验本地代码、清单、日志、数据边界和指标；用户批准后才实施代码或启动实验。
- 实验分为`DIAGNOSTIC`（诊断）、`QUICK_ABLATION`（快速消融）和`FULL_RUN`（完整运行）。目标测试金标只能用于完成训练后的最终报告，不能用于选择模块、候选、阈值、权重或检查点。
- 每次同步必须明确实验结论、唯一下一步、复用/重跑边界和通过/停止门槛。ChatGPT建议不等于用户批准。
- GitHub（远程仓库）推送失败时，以本地Git（版本管理）提交、运行清单和项目文档为事实源；不得假设外部模型已经看到未推送代码。

## 1. 项目与当前流程

任务是跨域ASTE（方面级情感三元组抽取）：源域有标注，目标域训练语料无标签，最终从目标句抽取`(方面, 观点, 情感)`。

当前系统使用单个T5-base（T5基础模型）架构，流程为：

1. 源域监督训练抽取器；
2. 抽取目标无标签语料的伪标签；
3. 用标签到文本生成器构造目标增强候选；
4. 经过局部契约、双向NLI（自然语言推断）、extractor exact（抽取器精确回抽）和冲突拒绝；
5. joint-quota MILP selector（联合配额混合整数线性规划选择器）在`k=1`和预算内选样本；该部件作为固定基础设施保留，不再宣称独立gap收益；
6. 联合source、pseudo和augmentation训练最终模型，并使用DANN（领域对抗网络）和轻量情感对比约束；
7. source-dev（源域开发集）选模，目标金标只做最终评价。

当前不采用双生成器或教师—学生。`laptop14 -> rest15`冻结参数为伪标签权重0.75、DANN=0.03、增强总有效质量30；这些参数不能直接迁移到其他方向。

## 2. 六方向正式结果与BGCA差距

| 方向 | CD-C3DA正式最佳 | BGCA论文 | 差距 | 当前问题 |
|---|---:|---:|---:|---|
| `rest14 -> laptop14` | 52.54 | 53.64 | -1.10 | 精确率高、召回低，多三元组差距主导 |
| `rest15 -> laptop14` | 45.27 | 45.69 | -0.42 | 伪标签质量和多三元组召回偏低 |
| `rest16 -> laptop14` | **48.93** | 47.28 | **+1.65** | 唯一超过，作为保护基线 |
| `laptop14 -> rest14` | 56.94 | 65.27 | **-8.33** | 最大缺口，高精确率但严重漏检 |
| `laptop14 -> rest15` | **54.01** | 58.95 | -4.94 | 当前研究方向，多三元组和负面仍弱 |
| `laptop14 -> rest16` | 61.55 | 64.00 | -2.45 | 整体较强，负面与观点召回不足 |

BGCA原版逻辑`rest14 -> laptop14`单随机种子复现得到P/R/F1=56.14/51.38/53.65，与论文五次均值53.64接近。C3DA同方向为59.95/46.77/52.54：C3DA精确率高3.81个百分点、召回低4.61个百分点；single F1为55.73，高于BGCA的54.82，但multi F1/召回为50.43/40.90，低于BGCA的52.94/47.63。1.11分差距主要由multi召回支持，不能归因于中性或single。

跨方向共同证据：源域方面对目标测试三元组的直接覆盖只有2.0%–11.1%，观点覆盖为41.0%–59.5%；多三元组贡献大量FN（假阴性）；中性长期很弱，但近期仅作为辅助目标。

## 3. 当前研究基线

`laptop14 -> rest15`正式54.01运行：raw P/R/F1=56.98/51.34/54.01，fixed F1=55.53；正面/负面/中性F1=58.20/49.46/0；single/multi F1=58.04/50.21，multi recall=43.27%。

伪标签权重从0.65提高到0.75曾带来明显收益，但0.80不再提高，说明目标信号原本偏弱、现在已到平衡区；剩余4.94分不能继续通过全局增权解决。

## 4. 已经确认的机制事实

### 4.1 上游复杂结构供给不足

- `rest14 -> laptop14`候选漏斗中，exact池只有134个不同multi锚；在`k=1`和150/150目标下最多只能选268条，MILP（混合整数线性规划）能精确复现该上限，选择器不是短缺根因。
- one/two/3+的候选生存率随结构复杂度下降，但既有局部计划无法稳定产生数量充足、元素完整且多样的复杂候选。
- BGCA同方向保留449条完整标签增强，其中104条multi；C3DA同方向增强multi只有38条、3+仅4条。该差异支持检查完整序列供给，但不能证明BGCA任一绑定部件具有独立因果收益。

### 4.2 训练末端干预只能重新分配错误

- 结构权重、原锚回放、复杂样本双呈现、普通质量分层和NLL（负对数似然）排序均未通过门控。
- Step10 EOS（序列终止）损失把正EOS margin行率从41.41%降到3.03%，并相对Step8提升multi召回3.64个百分点，但计划外输出与保持门槛失败，最终F1仅53.29。
- 继续生成—合法性审计发现30条救回中28条是干净救回，说明提前EOS是局部原因；但整体计划外输出来自另一批样本，因此继续叠加终点或排序损失没有依据。

### 4.3 元素缺失是当前主层级，但encoder辅助头不是解法

锚句隔离held-out（留出集）中，Step8/Step10遗漏事件的`element_absent`分别占91.25%和82.26%，纯`relation_unbound`均为0。方面和观点缺失可以重叠，因此只能判定“元素层整体”，不能单独批准方面或配对损失。

Step11 ECAL（元素覆盖辅助损失）是有效但失败的快速消融：共享encoder增加方面/观点token角色头，只作用于357条source gold multi和99条exact增强multi。训练内99条multi的full/retention从43/66升至50/76，new仍为66；独立held-out元素缺失率从55.14%恶化到58.88%。最终raw P/R/F1=56.90/48.45/52.34，fixed F1=52.78，TP/FP/FN=235/178/250；相对54.01基线F1下降1.67、FN增加14、负面F1下降7.55、multi召回下降2.91个百分点。

结论：ECAL只改善已见增强的记忆，没有改善目标域新元素泛化。关闭全部encoder token角色辅助损失变体，但保留“元素缺失仍是瓶颈，解决位置应在候选供给、结构计划或文本实现”这一证据。

### 4.4 M2具备符号组合容量，但M3无法可靠实现

- E0将M2定位为第一重构对象；E1对同一目标伪标签库存构造551条来源可追溯、无符号冲突的完整组合计划，固定见证集可满足300条总量、150条multi和30条3+。该结果只证明标签层的确定性容量，不能证明锚句—供体语义兼容、文本可生成或可训练。
- E2对551个相同锚句做local/composition配对生成，仅改变计划标签。组合计划完整元素出现率8.53%，原锚三元组保持66.79%，全验证仅14条；277/551对输出完全相同。自然度与NLI通过主要来自复述原锚，不能替代结构正确。
- 失败漏斗在535个失败中识别出278个`condition_ignored`，占51.96%，领先第二类26.73个百分点且结构、长度、正负情感与新颖性控制后稳定。当前首要M3问题是条件跟随，不是M4验证器过严。

### 4.5 词面强约束能消除条件忽略，但不能形成合法结构

FP-LCR是预注册的一次性M3接口诊断：计划、模型、提示、beam、seed和每计划单输出均冻结，只强制全部方面/观点短语在解码中出现。条件忽略率从50.27%降到1.09%，证明干预确实改变了条件跟随；但供体三元组正确回抽仅26.86%，原锚保持36.48%，two/3+保持47.52%/18.27%，计划外输出62.61%，PPL中位比1.57，全验证容量47/47/4，相对E2 local的multi有效供给还下降0.54个百分点。

结论：M3不能把“元素词面出现”当作“关系与情感实现”。FP-LCR及其beam、长度、词边界、顺序、范围和重试变体永久关闭；M4验证器因正确拦截机械拼接而继续保留。

### 4.6 语义兼容和固定局部插入也不能弥补M3接口能力

独立MiniLM（小型语言模型）余弦相似度审计中，FP-LCR有效率high-low只提高6.50个百分点，供体回抽只提高4.23个百分点，控制结构、长度、情感和新颖性后有效率差仅2.45个百分点；unplanned虽减少10.72个百分点，但mid优于high，缺乏单调关系。因此语义兼容不是可操作的主瓶颈，不能据此筛选或替换供体。

受控局部子句插入把原锚核心逐字符保留，并只让同一生成器生成一个供体短子句后以固定`; `附加。两次生成完全一致，自然度PPL比0.614、NLI 99.09%均通过；但供体双元素仅29.40%，供体三元组回抽14.52%，锚三元组保持60.62%，cross/displaced 53.72%，计划外38.11%，有效总/multi/3+仅20/20/1。相对E2 local有效率下降8.17个百分点，bootstrap区间[-11.25,-5.26]且所有大控制桶均为非正。

结论：锚句字符不被改写不等于抽取结构被保留；自然、非矛盾的附加子句仍可能没有实现计划元素和关系。永久关闭局部插入的连接词、位置、长度、提示和重试变体。当前需要明确决定是否只做一次可编辑骨架审计，或结束这一轮M3接口路线。

## 5. 论文方法的采用与边界

- BGCA：保留目标伪标签、完整标签条件生成和回抽一致性思想；不恢复双生成器，因为缺失伪标签不会被反向生成自动补齐，且BGCA变量相互绑定。
- RSDA（关系结构数据增强方法）：优先借鉴label composition（标签组合）与NLI精炼，用于M2结构计划和M4验证；paraphrase（释义改写）后置。
- C3DA（跨域数据增强方法）：借鉴方面增强与极性增强分离、多方面构造优先和噪声过滤；其原任务不是ASTE，数值不可直接迁移。
- AG-CDSA（增强生成跨域情感方法）：借鉴生成后一致性和“完整模块先验收、成功后再内部消融”；不增加新的对抗目标。
- DAEGCN（领域自适应增强图卷积方法）：名词/形容词元素线索仅在E1证明目标元素库存不足时作为M1备选；不直接加入图网络。
- CDSA（跨域情感适配方法）：领域特定/任务相关表示只作为未来M1候选；当前不调整DANN或增加表示分解损失。

RSDA式“元素完整multi标签组合计划”已经完成符号层审计；当前直接研究对象转为M3计划到文本实现。论文模块仍不能同时混入一次实验。

## 6. 六模块研究架构

| 模块 | 职责 | 状态 |
|---|---|---|
| M1 目标知识获取 | 伪标签、元素库存、领域适配 | `KEEP_FROZEN_BOUNDED_AUXILIARY`；同分布可吸收，但无稳定外部增益或系统性梯度冲突证据 |
| M2 结构增强规划 | 决定保留/新增哪些完整三元组 | V2关系拓扑线已关闭；保留“共享节点不可忽略”的解释证据 |
| M3 目标域文本实现 | 把完整计划写入真实目标句 | V2接口全部关闭；V3只批准规范图计划接口的正式调用点dry-run |
| M4 一致性验证 | 符号、NLI、exact和冲突拒绝 | `KEEP_CORE`，硬门槛 |
| M5 选择与预算 | `k=1`、联合配额MILP和预算 | `CLOSE_M5_GAP_SELECTION_LINE`；gap/coverage/novelty路线关闭，仅保留选择基础设施 |
| M6 训练吸收与解码 | 联合训练、DANN和结构生成 | `NO_M6_DANN_INTERACTION_ROUTE`；0.03仅作为正式历史配置冻结 |

依赖关系：M2决定有没有完整结构，M3决定能否实现到文本，M4决定是否可信，M5只能从合格池选择，M6只能学习已经存在的数据。项目不再在M2/M3不合格时继续给M6叠加小损失。

## 7. E0–E5执行路线

### E0 模块贡献与潜力审计（已完成）

已完成，只读证据批准M2进入E1，M1/M4保留，M5/M6冻结。

### E1 结构计划供给与验证审计（已完成）

已完成551条组合计划和300/150/30符号见证集；状态为`SYMBOLIC_CAPACITY_ACCEPTED`，不等于可训练供给。

### E2 增强V2生成审计（已完成且失败）

软组合接口输出`NO_TEXT_REALIZATION_ROUTE`；FP-LCR、语义兼容关联和受控局部子句插入也分别关闭。E2及其后续M3诊断均没有提供可训练增强。

### E3 增强V2快速消融

状态为`BLOCKED_BY_E2`。只有新的M3文本实现通过全部无金标结构、自然性和容量门槛后，才允许用增强V2替换当前增强。

### E4 模块内部消融

只有E3成功后，依次单独去掉composition（结构组合）、真实句锚定或NLI，判断收益来源。完整模块未成功前不做这些小消融。

### E5 正式完整验收

E1–E3通过后才允许`full_from_scratch`（完整从头运行）、`reuse_depth=0`、`resume_count=0`。晋级门槛：raw F1至少54.51、multi recall至少43.27%、multi F1至少50.21、single F1至少57.54、precision至少56.48%。代表方向通过后再扩展六方向和至少3个随机种子。

## 8. 当前关闭与保留边界

关闭：`k=2`、双生成器、教师—学生、中性强加权、普通质量/结构权重、3+对two重分配、回放、复杂双呈现、NLL选择/加权、EOS与删除排序损失、ECAL全部变体、当前Step8组合软提示、FP-LCR全部变体、语义兼容筛选/换供体、受控局部插入全部参数变体、候选比例网格、M5的gap/coverage/novelty次目标与质量折中，以及供给不变时新增pairing/计数/结构损失。

保留：单生成器、`k=1`、目标真实句锚定、硬契约、双向NLI、extractor exact、冲突拒绝、冻结联合配额MILP、增强总质量30、伪标签权重0.75、DANN=0.03、source-dev选模、multi/3+无金标门控和中性辅助。DANN=0.03当前只是正式基线固定项，尚未获得独立收益或冲突定位。

## 9. 给ChatGPT的当前结论与唯一研究问题

M1成对快速消融已经完成。treatment相对control使固定100条伪标签留出raw F1从72.80升至81.40，但行级full exact只从64%升至72%，差值95%区间[-1,+18]；two full只提高6.67个百分点。与此同时source-dev raw F1下降1.04，最终目标raw F1只从52.42升至52.68，fixed反降1.01，multi召回下降1.45。按预注册规则输出`NO_CLEAN_M1_SINGLE_TWO_GAIN`，不能把目标+0.26当成调参依据。

随后完成只读M1源域—伪标签任务梯度冲突审计：single/two各30对、token预算严格匹配，只计算共享T5参数的ASTE任务梯度。初始source–pseudo相对source–source负余弦率差1.67个百分点，最终为0；bootstrap均未达到门槛，single/two方向不一致，输出`NO_M1_GRADIENT_CONFLICT_ROUTE`。因此M1最终定位为`KEEP_FROZEN_BOUNDED_AUXILIARY`：正式451条、0.75/0.28846有效权重和DANN=0.03保留不变，但关闭伪标签调权、扩范围、梯度筛选、梯度补偿和额外112条混入。

M5已经完成两级只读审计。第一项入口审计确认历史`gap_aware=true/false`同时改变贪心/联合MILP、方面/观点/中性配额和multi下限，且MILP目标仅为质量顺序平方，不存在可独立开关的gap项；同一1021条池中的98条替换只能作为联合硬约束改变候选构成的机制证据，输出`STOP_M5_ENTRY_NO_CLEAN_COUNTERFACTUAL`。第二项使用真正可分离的两阶段词典序MILP：Control精确复现Step2历史300个ID，Treatment保持相同硬约束和质量主目标42,527,978，再最大化551条目标伪标签的661个方面/观点元素覆盖。9项入口全部通过，但两臂仍为同一300个ID，替换0、multi替换0、加权覆盖均54.3478%，输出`CLOSE_M5_GAP_SELECTION_LINE`。这不是gap系数太小，而是同质量最优解没有覆盖自由度；M5永久降级为固定选择基础设施。

M6 `DANN–ASTE Gradient Interaction Entry Audit`（领域对抗—任务梯度交互入口审计）已完成。正式54.01模型、tokenizer、DANN头、final-train manifest和60对匹配样本全部通过入口校验，重复余弦误差为0。总体ASTE–DANN负余弦率50.00%，ASTE–ASTE控制33.33%，超额16.67个百分点且bootstrap 95%区间为[1.67,31.67]；但总体中位余弦退化仅0.052、逐对退化0.036，two组超额仅10个百分点且区间跨0。预注册强度和结构门槛失败，输出`NO_M6_DANN_INTERACTION_ROUTE`。这支持“存在轻度总体冲突倾向”，不支持“DANN是当前多三元组主瓶颈”，也不批准DANN=0.03对0、系数搜索或梯度修正训练。0.03仅按正式54.01历史配置冻结。

M4、M3-V2与M2最后入口审计已经闭合V2：M4继续保留exact；M3-V2因安全监督供给不足关闭；M2虽然整体共享节点率64.50%且解释M3共享元素失败297/297，但formal pseudo原始/控制后共享率39.71/38.92%，严格低于40%，输出`CLOSE_M2_RELATION_TOPOLOGY_LINE`。V2最终为`NO_TRAINING_ROUTE`。

V3候选机制曾是canonical bipartite graph plan serialization（规范二部图计划序列化）：shared aspect/opinion（共享方面/观点）各表示一次，三元组成为带polarity（情感极性）的关系边，只改变label-to-text generator的multi计划输入。监督来源和独立接口审计均通过，随后完成正式训练/推理调用点接入；但该路线最终因固定128-token边界关闭，从未进入训练。

Codex接入的两个正式调用点分别处理904条源域生成器训练输入和451条目标伪标签锚句推理输入，两者共享`v3_graph_plan_interface`；single和下游flat格式均保持不变。verbose V1空跑的15项门槛中仅长度失败：5条pseudo multi超过128，最长159。ChatGPT将其判定为有界接口失败，并只允许一次统一compact V2；V2的8项单元测试通过，但全量CPU tokenizer长度预检仍有ID 572/600两条pseudo two达到137/135 token。根据预注册规则输出`CLOSE_V3_GRAPH_PLAN_ROUTE`，不做第三版格式、逐行回退或提高长度；图基础设施只保留为审计和解释工具。

ChatGPT确认无需为已确定的V2长度失败再运行GPU，且给出唯一新入口：在正式54.01 flat label-to-text generator（平面标签到文本生成器）上审计AG-CDSA式FGSM输入嵌入扰动。该路线与DANN不同：只考察生成器对输入和伪标签噪声的局部鲁棒性，不声称直接解决3+完整召回。Codex已实现零更新审计：正式904条数据/整理调用全覆盖，single/two/3+各2条前向，固定`epsilon=0.01`与二分平均损失；检查扰动范数、padding、梯度、损失、重复性、批次及模型哈希、参数梯度和零更新。当前尚未运行正式GPU审计，不能批准训练或推断F1收益。
