# CD-C3DA 目标锚定双通道增强设计

> 更新时间：2026-08-13 15:30（北京时间）

## 1. 设计结论

当前增强不再以“生成固定数量的新句子”为目标，而以目标域真实句为锚点，分别进行方面编辑和观点/情感编辑。两个通道共享同一个现有生成器，不训练双生成器；中性仅是观点通道中的低预算子类。

每个锚句最多选择 `k` 条增强。`k` 是锚句级上限，由方向级消融决定，首轮比较 `k=1/2`。全局最终数量不固定；只有通过硬契约和质量验证的候选才能进入训练，不足时少选。

## 2. 数据流

```text
目标无标签句
→ 抽取器产生高置信伪标签
→ 选择真实目标句作为锚句
→ 方面通道 / 观点情感通道局部编辑
→ 编辑契约硬检查
→ 抽取器与NLI分层支持
→ 缺口感知联合选择（含每锚句k上限）
→ 分层权重和总权重限制
→ 与源域、目标伪标签合并训练
```

## 3. 方面通道

优先替换或补充目标域长尾方面。新方面必须出现在文本与标签中，旧方面按计划消失，原观点、情感和未编辑三元组保持。候选来源只能使用目标训练无标签语料、目标伪标签和源域训练信息，不能读取目标测试金标。

## 4. 观点/情感通道

优先补负面观点、缺失观点和否定表达。同情感观点改写必须保持方面与情感；情感变换必须同时改变观点和情感。中性只保留高精度、局部最小变化样本，不设置必须填满的配额。

## 5. 多三元组召回

多三元组句优先级高于单三元组句。编辑一个三元组时，其他三元组必须完整保留；不得把删除其他三元组后的句子视为有效增强。选择器应优先补第二及后续三元组、长尾方面、缺失观点和配对，但不能重复已有三元组制造表面数量。

## 6. 硬契约与质量层

硬拒绝条件包括：计划跨度未出现、旧跨度未按计划移除、情感冲突、未编辑三元组丢失、方面/观点不在文本、控制符泄漏或非有限质量值。

硬契约通过后分层：抽取器精确回抽且NLI无冲突为高质量；边界兼容或部分支持为中质量；支持不足只审计；冲突直接拒绝。抽取器只能提供支持，不能把失败编辑重新标成其他标签后保留。

## 7. 联合选择与权重

选择同时考虑锚句、编辑类型、多三元组、情感和质量。实现必须求解联合可行组合，不能用固定贪心顺序。选择后重新验证每锚句 `k`、实际比例和多三元组覆盖。

高质量权重大于中质量；同一锚句第二条候选可以降权；中性权重低于常规高质量增强；增强总有效权重设上限，防止候选数量增加后主导训练。

## 8. 使用边界

- 不改变48.93保护流程和独立验收现场。
- 不使用目标测试标签调参。
- 不训练双生成器或教师—学生网络。
- 不强制固定增强总数。
- 候选通过代表方向验证后才能扩展六方向。
