# CD-C3DA 下一阶段改进计划

> 更新时间：2026-08-13 16:30（北京时间）
> 当前研究主线：目标域真实句锚定、多三元组召回、强验证、缺口感知选择和方向自适应训练。
> 当前筛选基线：`laptop14 -> rest15` raw F1 = 54.01；保护基线：`rest16 -> laptop14` raw F1 = 48.93。
> BGCA 对照状态：原版代码已建立独立基线提交；可复现分支只增加观测、方向级恢复与 3070 8GB 兼容，不改变原始增强和训练判定。正式对照结果产生后再回填本计划。

## 1. 当前项目情况

项目目标是六个跨域 ASTE（方面级情感三元组抽取）方向分别超过 BGCA（双向生成跨域方法）。当前只在 `rest16 -> laptop14` 超过 BGCA 1.65；最大差距是 `laptop14 -> rest14` 的8.33。

已有实验说明：继续放宽伪标签、扩大增强数量、提高中性损失、增加生成器轮次或全方向统一提高伪标签权重都不能稳定解决问题。`laptop14 -> rest15` 把目标伪标签权重从0.65提高到0.75后，raw F1 从51.21提高到54.01，证明目标信号原来偏弱；但0.80不再提高，剩余差距不能继续靠权重解决。

三次目标锚定实验为52.38、52.29和53.94。步骤3虽然仍比54.01低0.07，但证明真实目标句锚定、严格编辑契约、联合配额求解和质量分层有潜力。下一步不是照搬完整步骤3，而是保留这些成功机制，改为多三元组与召回优先，并控制同一锚句的相关样本重复。

## 2. 总体要改什么

整体改进分为五个模块：

1. 让增强建立在真实目标域句子上，减少标签到文本整句生成带来的伪自然文本。
2. 把增强目标从“增加总样本数”改为“补足多三元组、长尾方面和负面观点的具体缺口”。
3. 用硬契约和分层验证保证编辑确实完成，抽取器只提供支持而不重新改写标签。
4. 用每锚句上限、软配额和分层权重控制重复样本与训练信号总量。
5. 按跨域方向分别校准参数，避免某一方向的最佳设置伤害其他方向。

近期不采用教师—学生网络、双生成器、PCGrad（梯度冲突投影）、复杂配对损失、覆盖损失或中性损失强增权。

## 3. 模块一：目标域真实句锚定增强

### 目标

从目标域高置信伪标签句出发，只修改一个局部方面或观点，保留原句上下文和未编辑三元组，使增强更接近真实目标域语言。

### 设计

- 锚句来自目标训练无标签数据的高置信伪标签，不读取目标测试金标。
- 每个候选只允许一种编辑：方面替换、同情感观点替换或受控情感变换。
- 每个锚句最多选 `k` 条增强。`k` 是每个锚句的上限，不是全局增强数量，也不是候选词数量。
- 首轮比较 `k=1` 和 `k=2`；`k=1` 是保守基线，`k=2` 只有在第二条候选属于不同编辑类型且明显补足结构缺口时才允许。
- 全局最终数量由合格候选、软配额、总权重上限和配方共同决定，不强制达到固定条数。
- 同一锚句的候选不得仅靠轻微表面差异重复进入训练。

### 验收

- 每锚句选中数不超过 `k`。
- 不足配额时少选，不用低质量样本补满。
- `k=2` 必须相对 `k=1` 带来独立增益，否则固定回 `k=1`。

## 4. 模块二：多三元组与召回导向候选构造

### 目标

优先减少 FN（假阴性），尤其是多三元组句中的方面和观点漏检，而不是继续追求更高总体精确率。

### 候选优先级

1. 多三元组锚句中缺失的第二或后续三元组。
2. 目标域高频但源域低覆盖的长尾方面。
3. 已有方面但观点缺失或配对不完整的候选。
4. 负面观点及否定结构。
5. 少量高精度中性；中性不占据主预算。

### 约束

- 多三元组候选必须保留所有未编辑三元组。
- 不能通过重复一个已有三元组伪装成多三元组补全。
- 不使用目标测试金标决定候选、阈值、配额或 `k`。

## 5. 模块三：局部编辑契约与分层验证

所有候选先经过硬契约：

- 新方面/观点真实出现在生成文本中。
- 被替换的旧跨度按任务要求消失。
- 目标情感与计划一致。
- 未编辑三元组全部保留。
- 文本和标签不含控制占位符。
- 方面、观点跨度存在于文本，且不出现标签冲突。

硬契约通过后才进入分层验证：

- 高质量：抽取器完整精确支持，且 NLI（自然语言推断）无冲突。
- 中质量：边界兼容或部分支持，但计划三元组与未编辑三元组仍满足契约。
- 审计层：支持不足，只保存分析，不进入正式训练。
- 拒绝层：情感冲突、跨度缺失、三元组丢失或控制符泄漏，直接删除。

抽取器不得把失败编辑重新贴成其他标签后保留。

## 6. 模块四：缺口感知软配额与分层训练权重

### 选择

- 配额按方面、非中性观点、多三元组和少量中性设置为范围或最低保护，不设置必须填满的硬数量。
- 选择器联合考虑编辑类型、多三元组、情感、质量和每锚句 `k` 上限，不能采用会错过可行组合的固定贪心顺序。
- 最终选择后重新验证实际比例；不满足时降低目标规模或明确停止。

### 权重

- 高质量常规增强权重大于中质量增强。
- 中性增强使用更低上限，反向 NLI 为 neutral（中立）时只能进入中质量，contradiction（矛盾）必须拒绝。
- 增强有效总权重设置上限，避免数量增加后总训练质量被增强数据主导。
- 同一锚句有两条增强时，可对第二条降权，防止相关样本重复放大。

## 7. 模块五：方向自适应训练与晋级

- `laptop14 -> rest15`：保留 DANN=0.03、目标伪标签高层权重0.75，增强模块必须相对54.01比较。
- `rest16 -> laptop14`：48.93流程受保护；0.75迁移失败，不能把该参数作为统一默认值。
- `rest15 -> laptop14`：重点改善方面发现和多三元组召回，参数改动应保守。
- `laptop14 -> rest14`：最大差距方向，重点提高召回，不再优化已经很高的精确率。
- `laptop14 -> rest16`：保护强结构表现，重点补负面观点。
- `rest14 -> laptop14`：控制增强误检，保护方面增强的最低供给。

候选模块先在单方向、单随机种子筛选；达到晋级门槛后做完整从头验收，再扩展三个代表方向，最后运行六方向至少3个随机种子。

## 8. 阶段一：建立召回诊断与 `k` 对照

方向：`laptop14 -> rest15`。复用唯一完整从头父运行的未受影响上游，只重跑从增强选择开始的真实下游。

实验：

- A：54.01正式基线。
- B：成功机制最小版，`k=1`。
- C：与B相同，`k=2`，第二条必须来自不同编辑类型或不同结构缺口。

同时输出：总体 P/R/F1、正负中性、单/多三元组、每锚句选择分布、编辑类型、质量层、有效总权重和 FN/FP 分类。

晋级门槛：raw F1 高于54.01；recall 高于51.34%；多三元组 recall 高于43.27%、F1高于50.21；precision 不低于56.5%；负面F1不低于49.0。中性改善只作加分项。

## 9. 阶段二：结构化召回增强

若阶段一仍主要漏检，则只增加多三元组与长尾方面候选，不改变最终训练优化器、DANN 或伪标签权重。一次只验证一个新来源，并保存从候选到最终预测的逐阶段去向。

优先顺序：多三元组缺失方面 → 缺失观点/配对 → 长尾方面 → 负面观点 → 少量中性。

若 recall 上升但 precision 下降导致 F1 不升，应提高验证强度或降低同锚句重复，不扩大候选总量。

## 10. 阶段三：跨方向验证

阶段一/二通过后按顺序运行：

1. `rest15 -> laptop14`：验证低伪标签质量和低召回。
2. `laptop14 -> rest14`：验证最大缺口及多三元组召回。
3. `laptop14 -> rest16`：检查强方向是否回退。

至少两个代表方向提升且第三个方向没有不可接受回退，才建立六方向配方。每个方向允许独立 `k`、配额和伪标签权重，但方法结构必须一致。

## 11. 实验复用与完整验收

- 修改增强选择时，可受控复用准备、抽取器、伪标签和生成器；必须重跑增强及真实下游。
- 修改最终训练权重时，可受控复用第1–8阶段，只重跑最终训练和评估。
- 快速消融只能来自一个 `reuse_depth=0`（复用深度0）的完整父运行，子运行不能再次作为父运行。
- 候选成为新最佳、用于 BGCA 正式比较、扩展六方向或合并 `master` 前，必须完整从原始数据运行。
- 正式运行默认由用户执行；代码完成后提供从 CMD（命令提示符）开始的完整单行命令。

## 12. 暂停路线

- 教师—学生网络和双生成器。
- 直接放宽伪标签阈值。
- 中性损失强增权或用低质量中性填配额。
- 固定增强总条数并强制补满。
- 全方向统一伪标签权重。
- pairing/coverage（配对/覆盖）复杂辅助损失。
- 单纯增加生成器训练轮次或固定选择最后检查点。
