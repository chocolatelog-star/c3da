# CD-C3DA 后续改进计划

更新状态（2026-08-08）：六组跨域基线已经完成；`rest14 -> laptop14` 观点软过滤250条和观点契约供给150条实验均已完成并拒绝。最新实验 raw/fixed F1（原始/修正F1）为51.94/54.45，低于同方向旧契约150条的52.60/55.09，因此250条阶段取消。项目终极目标调整为六个跨域方向分别超过对应 BGCA。

```text
docs\superpowers\specs\2026-07-28-c3da-dual-channel-augmentation-design_CN.md
docs\superpowers\plans\2026-07-28-c3da-dual-channel-augmentation-implementation_CN.md
```

本文继续保留六组基线、错误分析、伪标签、多三元组和中性专项的总体路线，不重复维护双通道增强的代码级步骤。

## 1. 计划基线

本计划以当前 `master` 的可复现最好流程为唯一正式基线。基线配方为 `configs\recipes\rest16_to_laptop14_best_v1.json`，正式验收运行是 `native-best-v2-training-semantic`。

| 指标 | 当前值 |
|---|---:|
| BGCA 论文 label-to-text（标签到文本）F1 | 47.28 |
| raw precision（原始精确率） | 58.31 |
| raw recall（原始召回率） | 42.14 |
| raw F1（原始 F1） | **48.93** |
| fixed F1（修正 F1） | 50.21 |
| 相对 BGCA raw F1（原始 F1） | **+1.65** |

当前主要问题不是精确率，而是召回率：neutral（中性）类别几乎不能正确抽取，多三元组 recall（召回率）约为 35.85%，总体 recall（召回率）只有 42.14%。

六组审计进一步确认：唯一超过 BGCA 的 `rest16 -> laptop14` 并非绝对性能最高，而是其 BGCA 门槛47.28最低。其余方向的差距分别为 `rest15 -> laptop14` -0.42、`rest14 -> laptop14` -1.10、`laptop14 -> rest16` -2.45、`laptop14 -> rest15` -7.31、`laptop14 -> rest14` -8.33。共同瓶颈是召回、neutral（中性）缺失和跨域方面词覆盖；不同方向还分别存在负面情感、多三元组或大目标集长尾问题。完整证据表维护在 `实验记录与模型索引_CN.md`。

## 2. 实验原则

1. 每个改进从当前 `master` 新建独立实验分支，一次只改变一个核心变量。
2. 正式运行必须从原始数据重新训练抽取器和生成器，并重新产生伪标签、增强数据、最终训练集和最终模型。
3. 禁止读取其他 `run_id`、历史工作树、历史模型、历史伪标签或历史增强产物。
4. 各跨域组合使用独立 recipe（实验配方）、命令记录和 manifest（运行清单），不得继承 `rest16 -> laptop14` 的黄金观察数量。
5. raw F1（原始 F1）是主指标；同时报告精确率、召回率、三种情感和单/多三元组指标。
6. 候选实验先运行一个 seed（随机种子）筛选；进入正式比较后至少运行 3 个 seed（随机种子），报告 mean +/- std（均值加减标准差）。
7. 只有完整 GPU（图形处理器）实验优于当前基线且能够重新复现，才允许请求合并到 `master`。
8. 最终接受标准不是六组平均超过 BGCA，而是六个方向各自超过对应 BGCA；单个 seed（随机种子）只用于候选筛选。

## 3. 阶段一：建立六组跨域基线

### 目标

验证当前流程是否具有跨方向普适性，避免只针对 `rest16 -> laptop14` 调优。

### 六组组合

1. `rest14 -> laptop14`
2. `rest15 -> laptop14`
3. `rest16 -> laptop14`
4. `laptop14 -> rest14`
5. `laptop14 -> rest15`
6. `laptop14 -> rest16`

### 工作内容

- 为其余组合建立独立 recipe（实验配方）和黄金观察字段；首次运行前没有黄金输出时，只校验外部输入和流程身份，不预设伪标签或增强数量。
- 使用当前十阶段原生入口，从头生成每个组合的全部训练产物。
- 汇总每组 raw/fixed（原始/修正）指标、情感指标、单/多三元组指标和相对 BGCA 差距。
- 保存每组完整命令、Git（版本管理）提交、环境、模型和数据 SHA256。

### 接受标准

- 六组均能独立完成并支持同一 `run_id` 断点恢复。
- 没有跨运行产物读取记录。
- 获得六组可比较基线；这一阶段不因单组分数低而修改模型。

## 4. 阶段二：自动化错误分析

### 目标

确定 FN（漏检）和 FP（误检）主要来自生成、筛选、配对、情感还是边界问题，再决定修改哪个模块。

### 分类维度

- aspect（方面词）遗漏或边界错误
- opinion（观点词）遗漏或边界错误
- 方面词与观点词配对错误
- sentiment（情感极性）错误
- 多三元组部分抽取
- 否定表达错误
- neutral（中性）与正负类别混淆
- 生成格式或后处理错误

### 输出

- 每个跨域组合单独生成中文错误分析 Markdown（文档）和结构化 JSON（数据文件）。
- 给出各错误类型数量、占全部 FN/FP（漏检/误检）的比例及代表样本。
- 分别比较源域开发集、目标域伪标签分析集和目标域测试集，判断错误首次出现的阶段。

### 接受标准

- 每个预测三元组和黄金三元组均可追溯到具体错误类别。
- 能明确选出贡献最大的两个召回瓶颈，后续实验只围绕这些瓶颈展开。

## 5. 阶段三：分层伪标签与置信度

### 目标

提高召回率，同时保持当前约 58.31% 的精确率。

### 计划改动

- 保留当前 `hp1 + complete_multi2_w025` 作为高置信层。
- 对被过滤候选记录拒绝原因，不再只保存最终保留结果。
- 比较多次生成、候选间 agreement（一致性）和抽取器回抽结果，形成连续置信度。
- 对核心三元组相同、只有边界轻微差异的候选使用规范化软匹配。
- 不简单切换到 `hp2`，不以匹配历史条数为目标，也不无条件加入三元组以上样本。

### 第一阶段接受标准

- raw recall（原始召回率）高于 42.14%。
- raw precision（原始精确率）不低于 58.0%。
- raw F1（原始 F1）高于 48.93%，首个里程碑为 50.0%。

### 拒绝标准

- 仅靠增加伪标签数量提高召回，但总体 raw F1（原始 F1）下降。
- 高置信伪标签被低置信候选大量稀释。

## 6. 阶段四：增强候选多样性与分层筛选

### 保留模块

- 当前 8 轮 best（最佳检查点）label-to-text（标签到文本）生成器
- masked_mutual（互相掩码）双通道
- NLI（自然语言推断）过滤
- 抽取器 round-trip（回抽一致性）过滤
- 当前增强权重0.20只作为中等质量层的参考值，不再保留固定150条筛选上限

### 计划改动

- 每组标签生成多个候选，增加句式、长度、否定方式和多观点结构的多样性；生成阶段按目标域需要控制正面、负面和中性的采样比例。
- 为方面、非中性观点、中性观点、完整双三元组和否定表达建立独立候选池；比例使用允许浮动的上下界，不硬凑固定条数。
- 强过滤层继续要求编辑契约、标签保真、未编辑三元组保持、NLI（自然语言推断）、回抽一致性和去重；任何硬失败候选直接丢弃。
- 对通过强过滤的候选按质量分为高、中、探索层。训练时使用样本权重控制单条损失贡献，不实现逐样本 optimizer learning rate（优化器学习率）。建议初始权重区间为高层0.25、中层0.15到0.20、探索层0.05到0.10，具体边界先用历史候选离线统计确定。
- 总保留数由实际通过强过滤且满足比例区间的候选决定；设置安全上限只用于3070 8 GB显存和训练时长保护，不作为必须填满的配额。
- 每个目标域根据六组基线选择不同的比例区间：laptop14优先补方面覆盖和少量可靠中性；rest15优先负面与多三元组；rest16优先负面并保持结构平衡；rest14优先长尾方面和多三元组召回。
- 对生成器检查点先比较标签保真率、回抽通过率和候选多样性，再进行下游实验；不再假设训练轮次越多越好。

### 接受标准

- 筛选后增强数据的标签保真率不低于当前基线。
- 与当前增强数据相比，新增候选覆盖更多高频 FN（漏检）类型。
- 报告每个质量层和情感桶的候选数、实际保留比例、平均权重和下游消融贡献，不以总条数达标作为成功条件。
- 最终 raw F1（原始 F1）提升，且提升不能只来自 fixed（修正）后处理。

## 7. 阶段五：多三元组专项改进

### 目标

提高完整抽取能力，不重复已经失败的 `complete_multi2_w035` 和 dynamic strict 3+（动态严格三元组以上）路线。

### 实验顺序

1. 仅增加完整双三元组候选多样性，保持权重 0.25。
2. 对只回抽出部分标签的候选进行缺失三元组诊断，不直接纳入训练。
3. 先运行已冻结的多候选最终解码 v1：保留 beam 4（束搜索宽度4）基础预测，另取 beam 6（束搜索宽度6）的6个候选；仅补入至少3路支持、原文方面/观点跨度完整、情感严格多数且不与基础方面—观点对冲突的三元组，每句最多1个。
4. 双三元组确有稳定收益后，再单独研究三元组以上样本，禁止混在同一消融实验中。

### 接受标准

- 多三元组 recall（召回率）从约 35.85% 提高到至少 38%。
- 多三元组 raw F1（原始 F1）与 recall（召回率）同时提高。
- 总体 raw F1（原始 F1）高于同次运行的 beam 4（束搜索宽度4）基础输出，raw precision（原始精确率）下降不超过1.0个百分点。
- 首个 `rest15 -> laptop14` 方向的合并后 raw F1（原始 F1）超过 BGCA 45.69；若失败，不使用目标测试金标反向搜索支持度。

## 8. 阶段六：neutral（中性）专项改进

### 原因

历史 neutral strong weighting（中性强增权）实验显著降低总体结果，说明问题不是简单的类别权重不足。

### 数据拆分

- 属性缺失或不存在
- 客观状态描述
- 弱评价或模糊评价
- 含否定词但不是负面情绪
- 标注边界或语义歧义

### 计划改动

- 先构造少量高质量最小对比样本，区分客观否定和负面评价。
- 分析当前情感对比损失中 neutral（中性）样本数量、梯度贡献和与正负原型的距离。
- 优先修正训练样本与标签边界，不先提高 neutral loss gain（中性损失增益）。

### 接受标准

- neutral F1（中性 F1）产生稳定、可重复的实质提升。
- positive/negative F1（正面/负面 F1）各自下降不超过 0.5 个百分点。
- 总体 raw F1（原始 F1）不下降。

## 9. 阶段七：正式综合验证

进入这一阶段的候选必须已经在单个 seed（随机种子）下超过当前基线。

- 在六组跨域组合上运行至少 3 个 seed（随机种子）。
- 报告每组和六组平均 raw precision/recall/F1（原始精确率/召回率/F1）、fixed F1（修正 F1）及标准差。
- 与 BGCA 和当前 `master` 同时比较。
- 保存完整失败运行，不删除模型或检查点；较差结果只在总览中标记建议清理。
- 只有六组平均结果提高、关键单组无不可接受退化且完整复现通过，才请求合并到 `master`。

## 10. 暂停投入的方向

以下方向已有负面实验，不作为近期主线：

- `hp2` 简单放宽
- neutral loss（中性损失）强增权
- mixed generator（混合生成器）
- 旧 coverage classification head（覆盖分类头）
- dynamic strict top ratio（动态严格顶部比例）
- `complete_multi2_w035`
- 无条件加入三元组以上伪标签
- 只增加生成器训练轮次并固定选择 last（最后检查点）

## 11. 推荐执行顺序

1. 六组当前基线、观点软过滤、观点契约供给、动态比例与质量分层实验均已完成；扩大增强数量的路线未通过门槛。
2. 目标方面候选发现 v1 和源域联合门槛 v2 均已完成诊断，但精度不足，所有候选禁止进入训练；暂不继续设计 v3 门槛。
3. 当前先运行保守多候选最终解码 v1。实现分支为 `feature/multi-candidate-decoding-v1`，功能提交为 `b1c3705`，命令行入口修复提交为 `f1581f7`，首方向为只差 BGCA 0.42的 `rest15 -> laptop14`。该版本保持单生成器和原十阶段训练不变，只修改最终评估；首次运行已完成第1–9阶段，第10阶段在推理前暴露参数注册错误，修复后重复原命令只恢复评估。
4. 若 v1 达到第7节全部门槛，再将完全相同的冻结规则扩展到高精度低召回的 `laptop14 -> rest14`；不得先针对目标金标调整候选数、支持度或每句补入上限。
5. 若 v1 失败，先根据已保存的支持度、拒绝原因和新增三元组后验精度判断是“没有可补候选”还是“补入候选噪声过高”，随后转向 `laptop14 -> rest15` 的伪标签权重、DANN（领域对抗神经网络）和源域/目标伪标签梯度冲突诊断。教师—学生网络暂不采用。
6. 单方向候选通过后再测试三个代表方向，最后运行六组、三个 seed（随机种子）的正式综合验证。

下一次主任务先读取本文档和根目录 `实验记录与模型索引_CN.md`。当前已获用户批准的任务是运行上述正式 GPU（图形处理器）命令并分析结果；在结果出来前不再增加模块或改变冻结门槛。
