# CD-C3DA 下一阶段改进计划

> 更新时间：2026-08-25 13:18（北京时间）
> 终极目标：六个跨域方向分别超过 BGCA（双向生成跨域方法）。
> 当前正式研究基线：`laptop14 -> rest15` raw P/R/F1 = 56.98/51.34/54.01，fixed F1 = 55.53。
> 当前保护基线：`rest16 -> laptop14` raw F1 = 48.93，fixed F1 = 50.21；不在保护现场继续试验。
> 当前执行位置：V3图计划已经永久关闭。正式54.01 flat（平面）生成器的AG-CDSA式FGSM（快速梯度符号法）嵌入扰动零更新入口审计25项门控全部通过；研究方案只保留一次固定`epsilon=0.01`、0.5/0.5损失的生成器FGSM快速消融，不批准参数搜索，也不得同时改变任何数据、生成或最终ASTE配置。当前只完成新模型交接批准，FGSM代码实施与实验启动仍须用户在新对话明确批准。

## 1. 当前项目情况

CD-C3DA是面向跨域ASTE（方面级情感三元组抽取）的T5-base（T5基础模型）生成式系统。当前流程仍是单生成器：源域抽取器产生目标伪标签，标签到文本生成器构造增强句，强验证与联合选择器筛选候选，最终模型联合源域、目标伪标签和增强数据训练，并使用DANN（领域对抗网络）与轻量情感对比约束。

六方向中只有`rest16 -> laptop14`的48.93超过BGCA；当前集中研究`laptop14 -> rest15`，因为它既有完整54.01基线，又能代表“伪标签已经较好，但多三元组召回仍不足”的问题。该方向距离BGCA 58.95仍差4.94。

已经确认的主要瓶颈不是单三元组能力，而是目标域完整多三元组供给与召回：正式54.01流程的multi F1/召回为50.21/43.27%。锚句隔离诊断进一步表明，遗漏的计划三元组绝大多数连方面或观点元素都没有进入预测集合，纯关系未绑定为0。因此研究重点仍是多三元组和召回，中性只作辅助。

M1最新证据补齐了一个关键边界：正式伪标签监督可使同分布留出集raw F1提高8.60个百分点，但行级完整复现仅提高8个百分点且置信区间跨0，同时source-dev下降1.04、目标multi召回下降1.45。源域—伪标签负梯度率相对自然source–source基准在初始/最终检查点只高1.67/0个百分点，不能解释外部退化。因此不再通过伪标签调权、筛选或梯度补偿解决召回。

M5已经得到干净否证。历史开关同时改变选择算法和硬配额，不能作为gap单变量；真正可分离的两阶段词典序MILP在9项入口全部通过后，Control与Treatment仍选择完全相同的300条，替换0、multi替换0、目标伪标签元素覆盖增量0。M6随后审计正式54.01模型中的真实DANN梯度：总体冲突频率略高，但强度不足，且two组不显著，因此不能把DANN认定为当前多三元组主瓶颈。M5/M6均不进入训练。

V2最后三项入口也已完成。M4去污染源域开发集审计表明复杂结构的表面误拒绝主要由标签长度与情感分布解释，硬exact继续冻结；M3-V2同句遮蔽只得到source 110/357和pseudo 80/136条安全监督，供给不足；M2图拓扑虽然能解释297/297个共享元素失败，但目标伪标签共享节点率39.71%、控制后38.92%，均未达到预注册40%，因此输出`CLOSE_M2_RELATION_TOPOLOGY_LINE`。V2最终状态为`NO_TRAINING_ROUTE`。

V3曾只保留一个新变量：把label-to-text generator（标签到文本生成器）的multi计划从平面三元组串改为规范方面—观点二部图；single逐字节不变，抽取器、伪标签和最终ASTE输出仍用原平面格式。正式调用点已在`48a401b`接入，并由`05ee4b0`分离904条源域训练输入和451条目标伪标签推理输入。verbose V1正式空跑仅长度失败，5条pseudo multi超过128；唯一允许的compact V2把超限降至2条，但ID 572/600仍为137/135 token。按预注册停止条件，V3图计划路线永久关闭，不得做第三版压缩、逐行flat回退、提高max length或改变tokenizer。

## 2. E0至局部子句插入的阶段结论

这一轮没有训练新模型，也没有读取目标测试；所有数字都是无金标机制诊断，不能当作目标F1。

| 阶段 | 关键结果 | 判定 |
|---|---|---|
| E0 模块贡献审计 | 合法同候选比较没有支持继续修改M6；历史exact池只有134个不同multi锚，held-out元素缺失82.26%–91.25% | `APPROVE_M2_FOR_E1` |
| E1 结构计划供给 | 551个锚形成来源可追溯、无符号冲突的组合计划；固定见证集可满足300条总量、150条multi和30条3+ | `SYMBOLIC_CAPACITY_ACCEPTED`，只证明符号容量 |
| E2 配对文本实现 | 完整计划元素出现47/551=8.53%，原锚三元组保持66.79%，全验证仅14条；277/551对输出完全相同 | `NO_TEXT_REALIZATION_ROUTE` |
| E2 失败漏斗 | 535个失败中`condition_ignored`为278个，占51.96%，领先第二类26.73个百分点且控制后稳定 | `CONDITION_FOLLOWING_BOTTLENECK_IDENTIFIED` |
| FP-LCR | 条件忽略50.27%→1.09%，但供体三元组回抽26.86%、原锚保持36.48%、计划外62.61%、PPL比1.57，全验证容量47/47/4 | `CLOSE_FP_LCR_ROUTE` |
| 语义兼容性关联 | FP-LCR有效率high-low +6.50个百分点、供体回抽+4.23；控制后有效率只+2.45，且mid优于high | `NO_SEMANTIC_COMPATIBILITY_ROUTE` |
| 受控局部子句插入 | 锚句字符保持100%，但供体双元素29.40%、供体回抽14.52%、锚回抽保持60.62%、计划外38.11%，有效容量20/20/1 | `CLOSE_CONTROLLED_LOCAL_INSERTION_ROUTE` |

E1说明目标伪标签库存“可以在标签层组合”，没有证明供体与锚句语义兼容，也没有证明冻结生成器能写出合法文本。E2把失败定位到M3：当前`planned_label + anchor_text`软提示经常被忽略。FP-LCR又证明，简单强制所有方面和观点词面出现虽然能让模型服从条件，却只是把词塞进句子，不能保证方面—观点绑定、情感、原结构保持和自然性。

因此永久关闭当前Step8软组合接口、FP-LCR机制族、语义相似度筛选/换供体，以及受控局部插入的连接词、位置、长度与重试变体。保留E1的551条计划、来源谱系、300/150/30符号见证集和M4强验证；它们只可作为后续诊断的固定输入，不能直接进入训练。连续失败说明当前瓶颈不是计划数量或自然度，而是冻结生成器无法同时实现新增元素、正确关系和原锚结构。

## 3. 项目改进总纲：六模块依赖链

项目从“训练末端连续加小损失”调整为“先验收完整模块，再在成功模块内部做消融”。六模块按依赖顺序管理：

| 模块 | 职责 | 当前证据 | 决策 |
|---|---|---|---|
| M1 目标知识获取 | 目标伪标签、目标元素库存和域适配 | 同分布吸收改善但未形成稳定source-dev/目标迁移收益；无系统性任务梯度冲突证据 | `KEEP_FROZEN_BOUNDED_AUXILIARY`（冻结有界辅助） |
| M2 结构增强规划 | 决定保留、新增哪些完整方面—观点—情感三元组 | E1已证明551条组合计划和300/150/30见证容量，但未证明语义兼容 | `SYMBOLIC_CAPACITY_ACCEPTED`，冻结 |
| M3 目标域文本实现 | 把完整计划写入真实目标域锚句 | 软提示、词汇强约束、语义兼容筛选和固定末尾子句插入均未提供足量合法文本 | `CURRENT_INTERFACE_LINE_CLOSED`（当前接口路线关闭） |
| M4 一致性验证 | 符号、双向NLI、extractor exact与冲突拒绝 | 能控制噪声，但不能制造缺失元素 | `KEEP_CORE`，作为硬门槛 |
| M5 选择与预算 | 在`k=1`和固定预算内选择候选 | 历史开关无干净反事实；可分离覆盖次目标在同质量最优值下产生0替换、0覆盖增量 | `CLOSE_M5_GAP_SELECTION_LINE`；仅保留联合配额MILP基础设施 |
| M6 训练吸收与解码 | 把已有结构学入最终模型 | 权重、回放、双呈现、NLL、EOS和ECAL均未晋级；DANN总体有轻度冲突倾向，但two组与冲突强度未达门槛 | `NO_M6_DANN_INTERACTION_ROUTE`；0.03仅冻结保留，不做开关、调权或梯度修正 |

核心关系是：M2决定“有没有完整结构”，M3决定“能否写成目标域句子”，M4决定“生成结果是否可信”，M5只在合格池中选，M6只能吸收已经存在的数据。M1冻结、M5关闭、M6的DANN交互路线关闭后，不能再靠下游小损失掩盖上游计划到文本失败。后续变量必须重新服从这一依赖顺序，并先通过无训练入口诊断。

## 4. 论文方法与本项目模块的对应

论文方法只作为模块设计证据，不直接照搬整套网络或论文数值。凡是任务目标不是ASTE（方面级情感三元组抽取）的工作，只能借鉴结构思想；任何论文启发都必须先经过本地无金标诊断，再决定是否训练。

### 4.1 BGCA（双向生成跨域方法）

可借鉴：text-to-label（文本到标签）目标伪标签、label-to-text（标签到文本）反向生成，以及生成标签与回抽标签的一致性过滤。这些设计支持M1目标知识获取、M3文本实现和M4一致性验证。

采用边界：BGCA的反向生成主要让文本匹配已有伪标签；如果伪标签本身缺少方面或观点，它不会自动发现未知三元组。BGCA同时绑定生成、过滤、共享模型和等权训练，单次复现不能证明某一部件具有独立因果收益，因此不恢复双生成器。

### 4.2 RSDA（关系结构数据增强方法）

可借鉴：NLI（自然语言推断）精炼、label composition（标签组合）、多方面结构合成，以及后置的paraphrase（释义改写）。它对M2结构计划和M4验证最直接，其中composition优先于单纯扩大释义数量。

论文证据中，去掉NLI、composition和paraphrase时ASTE F1分别从57.84降至51.47、54.94和57.22。该结果说明组合与验证值得优先研究，但不能直接当作CD-C3DA的预期增益；本项目必须保持单生成器、`k=1`和目标真实句锚定做独立验证。

### 4.3 C3DA（跨域数据增强方法）

可借鉴：把方面增强与极性增强分开，多方面构造优先于单纯情感翻转，并对增强数据做噪声过滤。它支持M2按方面、观点和情感显式规划，而不是继续用一个全局比例混合所有增强。

采用边界：原任务主要是方面情感分类，不是完整ASTE，因此只能借鉴增强规划思路，不能直接迁移其数值结论或输出结构。

### 4.4 AG-CDSA（增强生成跨域情感方法）

可借鉴：生成后的一致性过滤，以及先验收完整增强模块、成功后再做内部消融。这支持当前E1–E4（阶段1至阶段4）的顺序。

采用边界：其对抗训练不是当前第一变量，任务也不要求完整方面—观点—情感关系，因此不把新的对抗目标加入M6。

### 4.5 DAEGCN（领域自适应增强图卷积方法）

可借鉴：用名词、形容词等元素线索扩大目标域方面/观点库存，生成时尽量保持原句句法骨架，并把增强、过滤和域适配拆成独立模块。若E1因合法donor（供体）元素不足而停止，这些线索可进入M1目标元素库存审计。

采用边界：其主要任务是ATE/AESC（方面抽取/方面情感分类），不能证明元素线索足以恢复观点及三元组关系；因此不在E0前直接加入词性候选或图网络。

### 4.6 CDSA（跨域情感适配方法）

可借鉴：区分领域特定表示和任务相关表示，并利用目标无标签数据改善域不变特征。它只作为未来M1域适配重审的候选依据。

采用边界：CDSA属于粗粒度情感分类，不能直接解决当前多三元组元素供给；M2/M3未完成前不调整DANN或增加新的表示分解损失。

### 4.7 综合采用顺序

当前只直接采用三个论文启发：RSDA的完整标签组合用于M2候选方案；BGCA与AG-CDSA的一致性思想继续固化在M4硬验证；DAEGCN的元素线索只在E1证明元素库存不足时作为M1备选。paraphrase、对抗目标、图网络、双生成器和新的表示损失均不进入当前实现。

## 5. 保留部件与关闭路线

继续保留并冻结：

- 单生成器与T5标签格式；
- 每个锚句最多一条增强，即`k=1`；
- 目标真实句锚定；
- 局部硬契约、计划元素显式出现检查；
- 双向NLI、extractor exact和冲突拒绝；
- joint-quota MILP selector（联合配额混合整数线性规划选择器）作为冻结基础设施，不再宣称独立gap收益；
- 增强总有效质量30；
- `laptop14 -> rest15`的effective pseudo weight（有效伪标签权重）0.75与DANN=0.03；
- source-dev（源域开发集）选模；
- multi/3+无金标uptake（训练吸收）门控；
- neutral（中性）仅作辅助。

继续关闭：

- `k=2`、双生成器、教师—学生；
- 中性强加权、低质量中性填充；
- 普通高/中质量权重、single/multi结构权重、3+对two的2:1重分配；
- 原锚回放、复杂样本双dropout（随机失活）呈现；
- NLL阈值、排序、均值、混合分数和NLL训练权重；
- EOS边界损失、真实终点损失、删除序列排序和解码期EOS限制；
- ECAL及其所有配方和组合变体；
- 当前Step8的`planned_label + anchor_text`组合软接口；
- FP-LCR及其beam、长度、短语边界、顺序、范围和重试变体；
- 当前增强供给不变条件下继续叠加pairing（配对）、计数或结构损失；
- 用目标测试金标选择候选、阈值、模块或检查点。

## 6. 分阶段执行计划

### E0：项目模块贡献与潜力审计（已完成）

类型：`DIAGNOSTIC`（诊断）；不训练，不读取目标测试。

已读取正式54.01、Step3–Step11、`rest14 -> laptop14`完整迁移、候选漏斗、uptake、NLL、EOS、删除偏好和元素瓶颈等历史产物，并建立：

1. 六模块开关矩阵；
2. 合法的单变量比较对；
3. 每个模块已证实收益、已证实损害和无法隔离的未知贡献；
4. 每个模块的潜力等级与`KEEP_CORE / REDESIGN / DEFER / CLOSE`状态；
5. 是否正式批准M2进入E1。

只有相同父运行、相同数据、相同候选或明确单变量的比较才能计算边际收益。多变量运行只能作为组合证据。E0不得以目标测试细分结果选择模块，最终只能批准一个模块。

### E1：结构计划供给与验证审计（已完成）

E0批准M2后已执行。类型：`DIAGNOSTIC`；不训练，不读取目标测试。

唯一变量是结构计划构造方式：

- 基线：当前局部单跨度编辑计划；
- 新方案：目标域元素完整的multi组合计划。

新方案仍保持每锚最多一条候选。它改变的是候选内部计划的三元组组成，不是候选数量。每个计划必须显式记录原始保留三元组、待新增三元组、每个方面和观点的来源、情感、结构数、插入位置以及元素/关系冲突。

固定`laptop14 -> rest15`、seed 1000、同一目标伪标签、同一目标真实锚、单生成器、`k=1`、300预算、中性辅助、现有验证阈值、选择器和训练配置。

训练入口门槛：

- ID、锚句、计划和元素来源对齐100%；
- 每个新增三元组的方面、观点和情感100%可追溯；
- 现有NLI/exact后不同multi锚不少于150；
- 有效3+候选不少于30；
- 有效完整multi供给相对当前规划器提高至少15个百分点；
- anchor-cluster bootstrap（锚聚类自助法）差值下界大于0；
- 控制标签长度、编辑类型和情感后方向不反转；
- single有效供给下降不超过5个百分点；
- 任一情感占比不超过80%，3+不能全部来自同一情感或编辑类型；
- 计划外、元素冲突或关系冲突行率不超过10%。

任一核心门槛失败，输出`NO_TRAINING_ROUTE`（无训练路线），停止该规划器；不得降低15个百分点门槛、放松强验证或修改`k=1`强行进入训练。

### E2：增强V2生成审计（已完成但失败）

E1符号容量通过后已执行。类型：`DIAGNOSTIC`；只生成和验证候选，不训练最终模型。

唯一模块变化是把“局部单跨度增强”替换为“完整结构计划＋目标真实句锚定实现”。保持同一伪标签、冻结生成器权重、单生成器、`k=1`、NLI/exact、300预算、增强总质量30和下游训练配置。

结果未通过：计划元素显式出现率仅8.53%，有效容量14/14/1。失败漏斗确认首要问题是条件跟随；FP-LCR虽消除条件忽略，却因机械拼接失败；语义兼容性没有形成足够强且单调的关联；固定局部子句插入仅得到20/20/1条有效容量，并相对E2 local显著退化。当前不得调整选择权重补救，也不得把上述任一产物训练进最终模型。

### E3：增强V2快速消融

状态：`BLOCKED_BY_E2`（被E2阻断）。只有新的M3文本实现先通过同一无金标门槛，才允许用增强V2的300条候选替换当前增强；否则不训练。

无金标通过门槛：actual-multi full不低于63.7449%，retention不低于77.1678%，new不低于78%，unplanned不高于11%；3+ full/retention/new分别不低于33.33%/38.10%/71.43%，unplanned不高于19.05%；single retention必须100%。失败则不晋级，不用目标测试反推结构组合。

### E4：增强模块内部消融

仅在E3通过后执行，每次只去掉一个子部件：去掉composition（结构组合）、去掉目标真实句锚定、去掉NLI。目的是解释增强V2的收益来自规划、目标域实现还是质量控制；完整模块未通过前不做内部小消融。

### E5：正式完整验收

仅在E1、E2、E3全部通过后执行。`laptop14 -> rest15`、seed 1000、`full_from_scratch`（完整从头运行）、`reuse_depth=0`、`resume_count=0`，由source-dev选模，目标金标只在最终评价读取。

正式晋级门槛：raw F1至少54.51，multi recall至少43.27%，multi F1至少50.21，single F1至少57.54，precision至少56.48%。通过后在`rest14 -> laptop14`做反方向代表复现；两个代表方向都通过后，才扩展六方向和至少3个随机种子。

## 7. 复用与重跑边界

| 阶段 | 允许复用 | 必须重算或重跑 |
|---|---|---|
| E0 | 全部历史只读产物 | 模块矩阵、合法比较和贡献账本；已完成 |
| E1 | prepare、pseudo、目标锚句和元素库存 | 结构计划、冲突、供给统计、bootstrap和控制分析；已完成 |
| E2 | prepare、extractor、pseudo和冻结generator checkpoint（生成器检查点） | 新计划文本生成、NLI、exact和冲突统计；已完成且失败 |
| M1成对快速消融与梯度审计 | 正式54.01流程的源域904条、正式伪标签451条及固定留出100条 | 已完成；成对训练从同一T5-base重跑，梯度审计只读复用初始与treatment最终检查点。结果均未晋级，后续不得以其为父运行继续训练 |
| M5两级选择入口审计 | Step1/2同一1021条候选池、历史300条清单、551条目标伪标签和Step2联合配额MILP | 已完成；历史接口输出`STOP_M5_ENTRY_NO_CLEAN_COUNTERFACTUAL`，可分离目标输出`CLOSE_M5_GAP_SELECTION_LINE`。后续不得训练、调gap系数、做质量折中或配额搜索 |
| M6 DANN–ASTE梯度交互入口 | 正式54.01最终模型、tokenizer（分词器）、DANN头、final-train manifest（最终训练清单）和实际参与DANN的样本 | 已完成；14项入口全部通过，但强度与two组机制门槛失败，输出`NO_M6_DANN_INTERACTION_ROUTE`。不得据此启动DANN=0.03对0训练 |
| E3 | 若生成器权重与输入哈希不变，可受控复用至generator | augment及以后全部重跑 |
| E5 | 不允许父运行复用 | 全流程从原始数据运行 |

快速消融只能来自一个`reuse_depth=0`完整父运行，子运行不得再次成为父运行。候选若成为新最佳、用于BGCA正式比较、扩展六方向或合并`master`，必须完成完整从头验收。

## 8. 当前结论与唯一下一步

正式最佳仍是54.01。V2的M1–M6均已完成判定：M1冻结为有界辅助，M2/M3没有形成合法文本监督，M4硬验证保留，M5仅保留联合配额MILP基础设施，M6的DANN=0.03只按历史配置冻结。不得恢复旧路线、降低门槛或做参数网格。

V3的正式调用点空跑与最后一次compact V2长度预检已经结束。V1有5条pseudo multi超限、最长159；V2仍有2条超限、最长137，因此输出`CLOSE_V3_GRAPH_PLAN_ROUTE`，不再消耗GPU做完整V2空跑。保留图构造、独立parser、调用追踪和长度审计作为只读基础设施，但不得把V3用于训练。

唯一候选下一步是实现并运行一次固定`Generator Embedding FGSM Quick Ablation`（生成器嵌入FGSM快速消融）。唯一变量是把正式904条flat生成器训练的clean loss（干净样本损失）替换为`0.5*(clean_loss+adversarial_loss)`，其中`epsilon=0.01`；生成器必须从同一T5-base（T5基础模型）完整重训，随后候选生成、NLI/exact（自然语言推断/精确回抽）、选择、最终训练、无金标uptake（吸收诊断）和一次目标测试全部重跑。prepare（准备）、extractor（抽取器）、正式目标伪标签及904条manifest（清单）可从唯一完整54.01父运行受控复用，复用边界止于generator（生成器）之前。必须冻结seed（随机种子）、fp16（半精度）、非重入梯度检查点、epoch/batch（轮次/批次）、优化器/调度器/选模、tokenizer（分词器）、prompt（提示）、长度、beam（束搜索）、`k=1`、预算、配额、M4、MILP（混合整数线性规划）、pseudo weight（伪标签权重）、增强质量30、DANN=0.03和最终ASTE配置；不得加入V3、EOS（序列结束符）、ECAL或其他损失。该方案目前等待用户在新Luna对话明确批准实施；交接批准本身不构成实验批准。

无金标晋级要求：固定计划上的multi exact相对control提高至少15个百分点且锚聚类bootstrap下界大于0；3+不反转；multi full/new各提高至少5个百分点，retention下降不超过2个百分点、unplanned-row上升不超过2个百分点；single full/retention下降均不超过2个百分点；source-dev raw F1下降不超过0.5个百分点；300预算、`k=1`和契约守恒全部通过。任一项失败输出`NO_FGSM_GENERATOR_ROUTE`并关闭FGSM参数搜索。最终测试只报告一次；正式晋级还须raw F1≥54.51、multi recall≥43.27%、multi F1≥50.21、single F1≥57.54、precision≥56.48%、fixed F1≥55.53。
