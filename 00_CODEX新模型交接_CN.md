# CD-C3DA Codex 新模型交接快照

> 更新时间：2026-08-25 13:18（北京时间）
> 接收模型：GPT-5.6 Luna（轻量高效率模型）
> 用途：新 Codex 对话首次接手时的唯一入口。它是当前状态快照，不替代实验索引、改进计划、项目 Skill（技能）和 Git（版本管理）历史。

## 1. 首次读取顺序与事实优先级

新对话必须按顺序完整读取：

1. 本文件；
2. `实验记录与模型索引_CN.md`；
3. `03_CD-C3DA下一阶段改进计划_CN.md`；
4. `docs/skills/c3da-experiment-workflow/SKILL.md`；
5. `07_CD-C3DA六组跨域实验详细分析与GPT交接_CN.md`。

事实冲突时依次以当前用户指令、`AGENTS.md`、正式项目 Skill、`03`、实验索引、`07`、本快照为准。不得只凭旧对话记忆覆盖仓库事实。

## 2. 项目目录与权限边界

- 正式仓库：`J:\nlp\CD-C3DA`，所有正式开发、实验、文档和Git历史均在这里。
- 当前开发工作树：`J:\nlp\CD-C3DA\.worktrees\candidate-completion-audit-v1`。
- 原始论文副本：`J:\nlp\C3DA-main`，只读参考。
- 48.93验收现场：`J:\nlp\CD-C3DA-native-best-rc-v1`，只读审计，不作为新实验输入。
- 用户未明确要求时不得启动GPU（图形处理器）实验；正式实验默认由用户运行。
- 未经用户明确许可不得删除文件、运行目录、模型或检查点。

## 3. 当前Git身份

- 保护主分支：`master=d2f2a35`。
- 正式GPU验收代码：`558e4de`。
- 当前开发分支：`feature/candidate-completion-audit-v1`。
- 交接文档更新前的开发基础头：`c0cc4fa`；交接提交后的当前头必须以`git rev-parse HEAD`核验。
- FGSM（快速梯度符号法）零更新实现：`34aa47c`。
- CUDA（统一计算设备架构）运行修复：`43f56f5`。
- 正式文档分支：`docs/account-migration-handoff-v1`。
- 交接文档更新前的正式文档基础头：`3dab2f4`；交接提交后的当前头必须以`git rev-parse HEAD`核验。
- 外部研究ChatGPT（聊天模型）对话：`6a7d8351-cbe4-83ea-9c7e-5b0af3afb4d9`。

新模型首次行动只能核验这些身份和工作区清洁状态；不得擅自合并`master`。

## 4. 六方向正式最佳与BGCA差距

| 方向 | CD-C3DA raw F1 | BGCA F1 | 差距 |
|---|---:|---:|---:|
| `rest14 -> laptop14` | 52.54 | 53.64 | -1.10 |
| `rest15 -> laptop14` | 45.27 | 45.69 | -0.42 |
| `rest16 -> laptop14` | **48.93** | 47.28 | **+1.65** |
| `laptop14 -> rest14` | 56.94 | 65.27 | -8.33 |
| `laptop14 -> rest15` | **54.01** | 58.95 | -4.94 |
| `laptop14 -> rest16` | 61.55 | 64.00 | -2.45 |

`rest16 -> laptop14`的48.93是唯一超过BGCA的保护基线，不在独立验收现场继续试验。当前研究方向是`laptop14 -> rest15`，正式基线raw P/R/F1为56.98/51.34/54.01，fixed F1为55.53，multi F1/recall为50.21/43.27%。终极目标是六个方向分别超过BGCA，不能以平均值或单方向替代。

## 5. 当前研究判断

共同瓶颈仍是目标域元素供给、完整多三元组实现和召回。中性只作辅助，不是当前主线。

已经完成并关闭：

- V2六模块入口链没有形成合法训练路线；
- V3规范二部图计划因固定128-token边界失败而永久关闭，从未训练；
- 双生成器、教师—学生、`k=2`；
- 中性强加权、普通结构/质量权重和比例搜索；
- 原锚回放、复杂样本重复呈现；
- NLL（负对数似然）排序/加权、EOS（序列终止）损失、ECAL（元素覆盖辅助损失）；
- FP-LCR（冻结计划词汇约束实现）、语义兼容筛选和局部子句插入；
- DANN（领域对抗网络）系数搜索及梯度修正；
- 供给不变时新增pairing（配对）、计数或结构损失。

继续冻结保留：单生成器、flat标签格式、`k=1`、目标真实句锚定、硬契约、双向NLI（自然语言推断）、extractor exact（抽取器精确回抽）、冲突拒绝、联合配额MILP（混合整数线性规划）、增强总有效质量30、伪标签权重0.75、DANN=0.03、source-dev（源域开发集）选模和multi/3+无金标门控。

## 6. 当前唯一候选研究动作

FGSM零更新入口诊断已在以下目录完成：

`J:\nlp\CD-C3DA\runs\diagnostics\laptop14_to_rest15_generator_fgsm_entry_audit_v1`

结果：25项门控全部通过，904/904数据与collator（批整理器）覆盖，single/two/3+各2条，扰动L∞=0.01，padding扰动0，重复损失逐值一致，参数更新和目标金标访问均为0，峰值reserved显存约1.465 GiB。该结果只证明工程与数值入口可用，不证明F1收益。

ChatGPT建议且Codex已核验的唯一下一研究变量是一次固定生成器FGSM快速消融：

[
L=0.5(L_{clean}+L_{FGSM}),\qquad \epsilon=0.01
]

不得搜索epsilon、损失比例或作用范围，不得同时加入V3、EOS、ECAL或其他机制。

重要审批状态：用户已经批准本次新模型交接文档工作，但尚未在新对话中明确批准实施FGSM正式训练代码。Luna不得把“ChatGPT建议”和“入口门控通过”误当成用户已经批准代码实施。

## 7. FGSM快速消融的冻结项和复用边界

必须冻结：904条flat生成器训练清单及顺序、T5-base初始化、seed 1000、fp16（半精度）、非重入gradient checkpointing（梯度检查点）、训练轮次/批次/优化器/调度器/选模、tokenizer（分词器）、prompt（提示）、最大长度、beam（束搜索）、`k=1`、预算、配额、M4、MILP、伪标签权重0.75、增强质量30、DANN=0.03及最终ASTE配置。

允许从唯一完整54.01父运行受控复用：prepare、extractor、正式目标伪标签和904条生成器训练manifest（清单）。必须从同一T5-base重训generator（生成器），并重跑候选生成、NLI/exact、选择、final data（最终数据）、最终ASTE训练、无金标uptake（吸收诊断）和一次目标测试。复用深度只能为1，子运行不得再作为父运行。

任何完成模型训练的实验都必须最终报告raw/fixed F1、P/R、TP/FP/FN、正负中性和single/multi细分。无金标门控失败只阻止晋级，不能阻止最终测试；目标金标不得用于反调参数。

## 8. Luna工作边界

Luna适合执行已经批准的明确任务：实现代码、补必要测试、做静态审查、读取日志、生成单行CMD（命令提示符）、维护双目录文档、提交和推送。默认主智能体串行，不为简单检查或重复验证创建子智能体。

Luna不得自行改变研究变量、降低门槛、开参数网格、启动GPU实验、删除文件、合并主分支、重新开放关闭路线，或把无金标代理指标解释为目标F1。遇到研究方向变化、门槛争议、复现异常或新最佳候选时，先做本地证据核验，再同步外部ChatGPT完整分析，最后由用户批准。

## 9. 新对话第一次回复要求

读取上述文件并核验Git后，只向用户报告：

1. 当前六方向最佳和最大差距；
2. 当前研究基线与主要瓶颈；
3. 当前唯一下一变量；
4. 已关闭路线；
5. 复用与必须重跑边界；
6. 当前审批状态和需要用户确认的下一动作。

第一次回复不得修改代码、运行实验或创建新研究方案。
