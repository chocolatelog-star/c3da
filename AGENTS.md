# 项目代理规则

> 更新时间：2026-08-30 23:17（北京时间）

新对话首次接手必须按以下顺序读取并完成只读核验：

1. `AGENTS.md`
2. `.ai/PROJECT_STATE.md`
3. `.ai/CURRENT_TASK.md`
4. `.ai/DECISION_LOG.md`
5. `docs/skills/c3da-experiment-workflow/SKILL.md`

实验记录与模型索引、`03` 和 `07` 按当前任务需要读取；详细改进设计统一维护在 `03`，服务器路径与运行约束统一维护在 `服务器迁移与实验运行交接_CN.md`。不再创建账号交接或正式 Skill（技能）的重复镜像文档。

若任务涉及服务器运行，必须在上述五项之后继续读取 `服务器迁移与实验运行交接_CN.md`；若任务涉及论文撰写，继续读取 `docs/skills/c3da-paper-writing/SKILL.md` 和其中按任务路由的 `references/` 文件。完成读取后还必须核验正式仓库、当前开发工作树、服务器提交身份和当前运行目录，文档陈述不能替代 Git（版本管理）与产物身份检查。

适用范围包括代码修改、模型训练、实验复现、断点恢复、命令生成、结果分析、文档更新、Git（版本管理）操作和文件清理。项目 Skill（技能）与本文件冲突时，以本文件和用户当前明确要求为准。

## 最高优先级与截止日期

当前项目最高优先级是在2026年9月16日前完成并提交ICASSP 2027（IEEE国际声学、语音与信号处理会议）常规论文。交付范围不是初稿，而是最终方法、正式实验、必要多随机种子、消融与基线对比、英文4页技术正文、可选第5页参考文献/资助/伦理声明、图表、引文、附录材料、IEEE模板检查和正式提交全部完成。

研究路线必须服从投稿关键路径：2026年9月8日前冻结方法，9月12日前冻结全部论文数字，9月15日前完成PDF（便携式文档格式）和提交演练。任何可能突破这些时间边界的新模块、参数搜索或非必要重构，必须先说明对投稿的影响并得到用户明确批准。

`J:\nlp\CD-C3DA` 是唯一正式开发仓库。`J:\nlp\C3DA-main` 是无 Git 的原始论文代码副本，`J:\nlp\CD-C3DA-native-best-rc-v1` 是最佳流程验收现场；后两者只用于审计，不得继续开发或作为正式新实验输入。

## 固定协作角色

- **Chat Sol（研究负责人）**：实验设计、路线选择、研究假设、无金标 Gate（门控）判定和新的研究决策。
- **Codex Luna（默认执行器）**：普通代码、诊断、测试、脚本、文档和普通故障处理。
- **Codex Sol（高级工程模型）**：仅处理复杂故障、核心训练逻辑、高风险修改、多文件复杂问题和代码审查。
- **Work Luna（重文件分析模型）**：仅处理大量 JSON（结构化数据）、CSV（逗号分隔数据）、日志、预测文件、多运行对比、配对分析、bootstrap（自助采样）和 failure attribution（失败归因）。

## 核心规则

核心规则：先读后问，关键歧义必问。

能从仓库可靠确认的事实不得重复询问用户或Chat Sol。但“能读”不等于允许自行推断需求。若存在不理解、信息冲突、多个实现会改变实验含义，或操作会影响研究范围、数据边界、阶段复用、成本、结果解释和安全性，必须先向用户说明理解、选项和推荐方案，并取得明确确认后再执行。

Chat Sol负责提出研究建议，Codex Sol负责复杂工程审查，用户始终是需求、实验和研究路线的最终批准者。

## Codex Luna 自查与 Codex Sol 工程验收

Codex Luna（默认执行器）完成普通实现后必须先自查。只有核心训练逻辑、数据/增强/筛选、复用与评估、泄漏风险、多文件高风险修改以及正式合并前，才升级 Codex Sol（高级工程模型）进行只读工程验收。

Codex Sol 只审查代码正确性、实验语义、数据边界和回归风险，不选择研究路线。验收输出限定为 PASS、PASS_WITH_NOTES 或 BLOCKED，并列出必要修改。

实验结果的研究有效性、Gate（门控）和下一变量仍由 Chat Sol（研究负责人）判断。不得让 Chat Sol 代替 Codex Sol 做日常代码审查，也不得让 Codex Sol 自行改变研究方向。

只有需要新的研究决策、真正研究歧义、不同实现会改变实验含义、继续可能违反实验协议、实验结果需要研究判断或需要扩大范围时，才能询问 Chat Sol。提问必须使用：

```
TASK:
<TASK_ID>

FACT:
<直接相关事实>

BLOCKER:
<具体歧义>

OPTIONS:
A:
B:

CODEX RECOMMENDATION:
<推荐方案和一句理由>

QUESTION:
<需要决定的问题>
```

Chat Sol 不能直接读取 J 盘，只能读取已经推送到 GitHub（远程代码仓库）的文件或用户上传的文件。本地未推送状态由 Codex 使用精简同步格式传递：RUN / CODE / CHANGE / REUSE / RESULT / BOUNDARY；不可观测项必须明确写为“当前不可观测”，不得假设 GitHub 已同步。

## 最小协作文档结构

必须保留 `AGENTS.md`、`.ai/PROJECT_STATE.md`、`.ai/CURRENT_TASK.md`、`.ai/DECISION_LOG.md`、`实验记录与模型索引_CN.md`、`03_CD-C3DA下一阶段改进计划_CN.md` 和正式 Skill。实验索引直接承担 RUN_INDEX（运行索引）职责，不创建重复的 `.ai/RUN_INDEX.md`。

`07_CD-C3DA六组跨域实验详细分析与GPT交接_CN.md` 仅在协作边界或重大研究事实变化时更新。除复杂任务或大量文件分析外，不默认创建 `.ai/tasks` 或 `.ai/results` 下的任务、结果 Markdown（标记语言）文档；若 `.ai` 不存在，只创建上述三个控制文件。

为方便用户向 Chat Sol 上传本地文件，根目录保留 `CHAT_SOL_PROJECT_STATE_CN.md`、`CHAT_SOL_CURRENT_TASK_CN.md` 和 `CHAT_SOL_DECISION_LOG_CN.md` 三份上传镜像。它们不是事实源，内容分别镜像 `.ai/PROJECT_STATE.md`、`.ai/CURRENT_TASK.md` 和 `.ai/DECISION_LOG.md`；任一源文件变化时，必须在正式仓库和当前开发工作树同步刷新对应镜像并检查规范化文本一致性。

`CURRENT_TASK.md` 只能存在一个当前任务，状态只能是 `DRAFT`（草案）、`APPROVED`（已批准）、`RUNNING`（执行中）、`BLOCKED`（阻塞）或 `COMPLETE`（完成）。ChatGPT建议不等于用户批准。

## 项目 Skill 与环境 Skill

本仓库只有两项正式项目 Skill（技能）：

- `docs/skills/c3da-experiment-workflow/SKILL.md`：代码、实验、复用、命令、结果分析、文档和清理任务的强制工作流；
- `docs/skills/c3da-paper-writing/SKILL.md`：论文、表格、引文、主张证据和投稿材料的写作工作流，只在论文任务中启用。

个人或系统级 Skill 会随 Codex（代码智能体）环境变化，不是项目事实源，不把其完整名称复制进项目状态。接手时可从 `C:\Users\wwh\.codex\skills`、`C:\Users\wwh\.agents\skills` 和当前会话的 Available Skills（可用技能）清单发现；只有上述两项仓库内 Skill 决定本项目的长期规则。

本地 CPU（中央处理器）测试使用 `J:\conda\envs\c3da\python.exe -m pytest -p no:cacheprovider`。当前核验版本为 `pytest 8.3.4`，并固定在 `requirements-rtx3070.txt`；禁止使用来源不明的系统 `pytest.exe` 代替项目环境解释器。

## 实验与审批边界

- 用户没有明确说“你来跑”或“帮我直接跑”时，只修改代码或文档，做必要小测试，并给出从 CMD（命令提示符）开始的完整单行命令。
- “确定”“继续”“按你说的做”只批准当前实现，不构成代跑授权。
- 不启动 GPU（图形处理器）实验、不删除文件、不合并分支、不推送，除非用户另行明确批准。
- 保留 RTX 3070 8GB（8 GB 显存）约束、版本兼容、进度条、断点恢复、Git 工作树规则和阶段复用规则。
- 目标测试金标只能用于完成训练后的最终报告，不能用于选择候选、阈值、权重、模块或检查点。
- 每次实验、诊断、失败、程序报错或 Gate 未通过后，必须交付实验结论、机制判断、与基线/BGCA（双向生成跨域方法）的比较、唯一下一步建议和 ChatGPT 同步状态。

## 服务器利用与交付效率

- 当前主要实验在RTX 4090约24 GB显存服务器运行；默认并行2个真正独立的实验。只有短烟雾测试证明单任务峰值显存不超过约7 GB、三任务合计后仍预留至少3 GB，并确认CPU（中央处理器）、内存、磁盘与数据加载不是瓶颈时，才允许并行3个。
- 并行不能改变冻结的batch（批大小）、梯度累积、随机种子、优化器或训练语义；每个任务必须使用独立运行目录、日志、检查点和恢复状态，禁止共享可写缓存或混用父运行。
- 预计代码实现、审查或本地测试还需30分钟以上且服务器没有已批准实验可跑时，Codex必须主动告诉用户：继续GPU模式、切换无GPU模式或关机中的推荐选择及预计下一次需要GPU的时间。不得让付费GPU服务器长时间空等代码。
- 建议开启GPU服务器前必须达到“提交后可直接运行”：本地CPU测试通过、复杂改动完成Codex Sol（高级工程模型）复审、功能提交已推送、服务器目标提交明确、命令和输出目录已冻结、恢复语义已核验。
- 正式长运行前先做5至10步GPU烟雾测试，检查显存、数值有限性、进度、日志、输出路径和恢复入口；烟雾测试通过后使用新正式目录启动，不把烟雾产物混入正式运行。
- 服务器有运行任务时不得在同一工作目录切换分支、拉取新提交或修改代码。GPU运行期间，本地可以继续论文写作、结果整理和下一任务CPU实现；下一版代码完成后等待现有任务结束或使用隔离工作树部署。

## 双目录同步与安全修改

正式阅读入口与当前开发工作树中的对应协作文档必须保持规范化文本一致；同步时保留两边已有用户改动，不覆盖、不猜测，并分别检查 Git diff（差异）和 Git status（状态）。

删除任何文件、模型、检查点或运行目录都需要用户明确许可。模型默认从 `J:\nlp\models` 查找，下载只保留 PyTorch（深度学习框架）版本。项目 Skill 与本文件冲突时，以本文件和用户当前明确要求为准。
