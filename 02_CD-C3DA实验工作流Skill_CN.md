# C3DA 实验工作流 Skill（技能）中文镜像

> 更新时间：2026-08-23 23:55（北京时间）
> 正式生效文件：`docs/skills/c3da-experiment-workflow/SKILL.md`。本文件只便于人工阅读，修改时必须与正式 Skill 同步。

## 日常读取顺序

每次代码改动、实验设计、结果分析或清理前，完整读取：

1. `实验记录与模型索引_CN.md`
2. `03_CD-C3DA下一阶段改进计划_CN.md`
3. 正式项目 Skill

只有 GPT（生成式预训练模型）交接内容变化时按需读取和更新 `07`；详细改进设计统一维护在 `03`。

## 核心规则

- 六方向分别超过 BGCA（双向生成跨域方法）；保护48.93，研究基线54.01。
- 当前主线采用六模块依赖链：M1目标知识获取冻结，M2结构增强规划优先重构，M3目标域文本实现等待M2，M4一致性验证保留为硬门槛，M5选择预算冻结，M6训练吸收暂停新增损失。
- 当前执行顺序固定为E0模块贡献审计→E1结构计划供给→E2增强V2生成→E3快速消融→E4内部消融→E5完整验收；E0完成前不训练、不读取目标测试、不提前实现增强V2。
- 代码修改前说明一次详细方案并获得确认；正式实验只有用户明确要求时才启动。
- 默认主智能体串行执行；只有能同时节省时间和 Token（令牌）的独立耗时任务才并行，默认最多两个子智能体。
- 快速消融只能从一个完整父运行受控复用；禁止复用链、多个父运行和手工拼接。
- 新最佳、BGCA比较、六方向、多随机种子或合并主分支前必须完整从头验收。
- RTX 3070 8GB，模型优先从 `J:\nlp\models` 查找，多步 CMD（命令提示符）命令必须合并为一行。
- 每次重要改进或实验结束后整体更新索引和 `03`；按需更新 `02/07`，所有文档开头写更新时间。
- 文档必须双目录同步：`J:\nlp\CD-C3DA`是用户正式阅读入口，当前开发工作树保存实验分支事实。每次修改索引、`02`、`03`、`07`或正式Skill（技能），同一回合必须同步两个目录、做规范化文本一致性检查，并分别提交对应分支；不得只更新工作树。若发现用户未提交修改或不能安全同步，先报告而不覆盖。
- 实验结束后无需等待用户追问，Codex 必须主动完成：本地有效性核验与初步分析 → 同步 ChatGPT 并等待完整回复 → 本地可实施性复核 → 向用户给出综合实验分析和唯一下一步。不得只报告进程结束或指标。
- 每次实验或诊断的最终交付必须显式列出“实验结论”“唯一下一步建议”“ChatGPT 同步状态”；同步请求也必须要求 ChatGPT 返回结论和唯一下一步，不能把这些内容留给用户追问。
- 所有完成模型训练的实验都必须运行最终目标测试并报告 raw/fixed F1、P/R、FP/FN、情感和单/多三元组细分。无金标机制门控仍在评价前执行并保存，但失败只阻止机制晋级，不再阻断最终测试；失败结果标记为“门控失败、测试仅供报告”，不得用目标金标回改本轮参数或覆盖预注册失败。纯诊断且没有训练模型的任务不要求目标测试。
- 综合交付至少包含有效性、基线/BGCA差距、分类与单/多三元组细分、FP/FN和机制判断、下一唯一变量及复用/重跑边界；代码或新实验仍须用户批准。
- 未经许可不删除任何文件。失败实验先保存指标、命令、清单、日志和首次错误。

## ChatGPT 与 Codex 同步

- ChatGPT（聊天模型）负责只读分析，明确区分 `DIAGNOSTIC`（诊断）、`QUICK_ABLATION`（快速消融）和 `FULL_RUN`（完整运行），检查泄漏与复用风险并提出研究方案。
- Codex（代码智能体）负责核验本地代码和运行证据；用户批准后才实施、测试、提供命令并维护文档。用户是唯一审批者。
- Codex 发出同步请求后必须等待 ChatGPT 完整回复；未完成时明确报告仍在等待，不得提前进入下一研究步骤或把自己的判断冒充 ChatGPT 结论。
- 结果先由 Codex 读取本地代码、清单、日志和逐条产物完成证据分析，再交 ChatGPT 做独立研究分析；ChatGPT 返回后 Codex 必须核验并主动汇总，不让用户再次追问“分析实验结果”和“下一步”。
- 回复完成后默认只精简转述核心结论、推荐路线、本地核验、代码改动和实验边界；只有用户要求详细解释时才展开全文，降低重复 Token（令牌）消耗。
- 始终区分 `ChatGPT 建议`、`Codex 已核验` 和 `用户已批准`。每完成一个可独立验证的研究模块就同步一次，不把多个研究变量混合跑完后才分析。
- 日常同步采用6行增量格式：运行、代码、唯一变化、复用、结果、数据边界；完整配置和日志只给路径。ChatGPT 日常回复只保留判定、最多3条证据、风险、唯一下一变量、复用边界和门槛。
- 只有新最佳、完整运行、BGCA正式比较、跨方向/多随机种子、复用或泄漏异常、混变量、无法复现或用户要求详细说明时才展开完整报告。
- 普通诊断/快速消融只更新实验索引；研究路线变化更新 `03`；新最佳、重大失败或跨方向结论更新 `07`；实验纪律改变才更新正式 Skill。用户明确要求的维护范围优先。
- 同步事实源为 GitHub（远程仓库）、实验索引、`03`、`07` 和正式 Skill。GitHub 推送失败时，由 Codex 从本地 Git（版本管理）、文档和实验产物提取必要字段直接转发；ChatGPT 不得假定自己看到了未推送代码。
- 每轮最少覆盖代码身份、实验身份、复用关系、数据边界、完整指标、机制漏斗和运行证据；缺失字段明确标记“当前不可观测”。ChatGPT 建议必须经 Codex 核验并由用户批准，不能直接执行。

完整细则以正式 Skill 为准。
